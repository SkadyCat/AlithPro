# 交付说明

- 源任务文件：`msg-2026-03-28T08-39-13-489Z.md`
- 目标：新增 `/dev` 页面，完整复用主页面界面，但只允许向 `Alith\sessions` 留言，并且启动动作使用 `Alith\run.bat`；其余行为与主页面保持一致。

## 已完成的改动

1. 复用了现有主页面路由回退机制，使 `/dev` 继续加载同一套主页面结构，无需额外复制 HTML/CSS 文件。
2. 在 `application\public\app.js` 中新增 `/dev` 模式识别逻辑。
3. 将 `/dev` 页面锁定到 `Workspace = Alith`：
   - 工作区列表仅显示 `Alith`
   - 不允许在 `/dev` 页面切换到其他工作区
   - 不允许在 `/dev` 页面新建其他工作区
4. 保持消息投递接口不变，但由于当前工作区被固定为 `Alith`，因此新建任务只会写入 `application\workspace\Alith\sessions\`。
5. 保持启动 Agent 的现有调用链不变，但由于 `/dev` 页面当前工作区固定为 `Alith`，启动按钮会沿用现有逻辑启动 `application\workspace\Alith\run.bat`。

## 关键实现说明

- `server.js` 已经对所有非 `/api/` 路径回退到 `public\index.html`，所以 `/dev` 可以直接复用主页面外壳。
- 真正的差异控制点在 `public\app.js`：通过 `window.location.pathname === '/dev'` 识别开发页模式，并把当前工作区、工作区列表、创建工作区行为都限制到 `Alith`。
- 这样实现后，页面布局、搜索、卡片区、详情区、模型切换、启动按钮等都与主页面保持一致，只是工作区上下文被固定为 `Alith`。

## 验证结果

- 已对 `application\public\app.js` 进行语法检查：`node --check public\app.js` 通过。
- 已运行现有项目验证命令：`npm test` 通过。
- 已确认 `application\workspace\Alith\` 存在，且其中包含 `sessions\` 与 `run.bat`，满足 `/dev` 页面的目标约束。

## 剩余风险或阻塞

- 无新增阻塞。
- `/dev` 页面通过“固定当前工作区”为 `Alith` 来满足“只允许向 Alith\sessions 留言”和“启动 Alith/run.bat”的要求；如果后续需要更显式的视觉标识（例如页面角标或标题提示），可再补充 UI 文案。
