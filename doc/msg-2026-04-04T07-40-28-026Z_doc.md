# 交付说明

- 源任务文件：`inprocess\msg-2026-04-04T07-40-28-026Z.md`
- 任务目标：扩充工作台中的模型选择列表，覆盖当前可用模型，并改为支持 JSON 配置。

## 所做改动

1. 新增 `application\model-presets.json`，把模型清单和默认模型迁移到独立 JSON 文件中，便于后续直接配置。
2. 更新 `application\server.js`：
   - 从 `model-presets.json` 读取模型配置；
   - 支持 `modelOptions` 对象数组和兼容旧的 `presets` 数组；
   - 在工作区摘要和设置保存接口中同时返回 `settings.presets` 与 `settings.modelOptions`。
3. 更新 `application\public\app.js`，让模型下拉框优先渲染 `modelOptions` 的 `value/label`，并兼容旧数据结构。

当前 JSON 模型清单已包含以下 15 个选项：

- `claude-sonnet-4.6`
- `claude-sonnet-4.5`
- `claude-haiku-4.5`
- `claude-opus-4.6`
- `claude-opus-4.6-fast`
- `claude-opus-4.5`
- `claude-sonnet-4`
- `gpt-5.4`
- `gpt-5.3-codex`
- `gpt-5.2-codex`
- `gpt-5.2`
- `gpt-5.1`
- `gpt-5.4-mini`
- `gpt-5-mini`
- `gpt-4.1`

## 验证结果

1. `application` 下现有脚本校验命令 `npm test` 已通过。
2. 在隔离端口 `7440` 启动本地服务后，请求 `/api/workspaces/test`，确认返回：
   - `settings.model` 为 `claude-sonnet-4.6`
   - `settings.presets` 包含 15 个模型值
   - `settings.modelOptions` 返回完整的 `value/label` 列表

## 剩余风险或阻塞项

无已知阻塞。若后续 CLI 模型标识有变更，可直接编辑 `application\model-presets.json` 同步更新。
