# 交付说明

- 源任务文件：`inprocess\msg-2026-04-04T08-35-40-348Z.md`
- 任务目标：修复模型下拉框仍显示旧选项、没有使用任务要求中的完整模型列表的问题。

## 所做改动

更新 `application\public\app.js`：

1. 新增前端内置的 15 项模型清单 `CLIENT_MODEL_OPTIONS`，内容与任务要求一致；
2. 新增 `buildModelCatalog()`，统一生成 `presets`、`modelOptions`、`defaultModel`；
3. 将 `state.modelCatalog` 默认值改为内置完整模型清单；
4. 即使 `/api/model-presets` 不可用或后端仍返回旧 `presets`，前端也会继续使用这份完整模型列表，而不是退回旧选项。

这样刷新页面后，模型下拉框会直接显示新的 15 个模型选项，不再依赖后端先升级成功。

## 验证结果

1. `node --check public\app.js` 已通过。
2. `npm test` 已通过。
3. 在隔离端口 `7443` 请求 `/api/workspaces/test`，确认 `settings.modelOptions` 仍返回 15 项，首项为 `claude-sonnet-4.6`，末项为 `gpt-4.1`。

## 剩余风险或阻塞项

- 无已知阻塞。
- 浏览器需要刷新一次，才能加载新的前端脚本并看到更新后的下拉列表。
