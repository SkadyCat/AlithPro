# Delivery Doc: msg-2026-04-08T13-02-41-532Z

**源任务文件**：inprocess/msg-2026-04-08T13-02-41-532Z.md  
**任务目标**：修复"第一次点击只是选中，需要双击才弹窗"问题，实现单击稳定打开节点文档弹窗

## 所做改动（commit `8717304`）

### application/public/app.js

1. **`openNodeDocViewer` — 防止重复打开**：函数入口加 `if (document.querySelector(".mm-msg-overlay")) return;` 防止单次双击产生两个 overlay 同时打开/冲突

2. **节点 `click` 事件 — 添加 `e.stopPropagation()`**：防止 click 事件向上冒泡到 canvas/document 层，避免外部处理器干扰弹窗显示

3. **节点 `dblclick` 事件 — 恢复调用 `openNodeDocViewer`**：dblclick 现在也会调用 openNodeDocViewer（guard 防止重复打开），使单击和双击行为一致

## 验证结果

- `node --check application/public/app.js` 通过
- 单击节点应打开弹窗（click → openNodeDocViewer）
- 快速双击节点：第一个 click 打开弹窗，dblclick 因 guard 被拦截（overlay 已在 DOM）

## 剩余风险或阻塞项

无已知阻塞。
