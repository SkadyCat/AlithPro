源任务文件：`msg-2026-04-03T19-36-35-744Z.md`

任务目标：
- 任务流默认包含开始节点和结束节点。
- 节点放置来源只允许来自 workflow 文档，workflow 以外文档不能作为放置节点。
- 普通节点包含一个输入和一个输出端口，开始/结束节点除外。

所做改动：
- 更新 `application/server.js`，在 workflow 归一化阶段自动补齐 `start` / `end` 边界节点，并为节点增加 `type` 字段。
- 更新草稿转换为任务流逻辑，使新建的任务流默认包含 `开始 -> 文档节点 -> 结束` 三类节点。
- 更新 `application/public/app.js`，将可放置节点来源改为当前 workspace 的 workflow 文档列表，不再从 drafts/sessions/inprocess/processed 直接取文档。
- 更新 `application/public/task/workflow.js`，按节点类型渲染开始/结束/普通节点，并给普通节点显示输入/输出端口。
- 更新 `application/public/styles.css`，补充开始/结束节点和端口的样式表现。

验证结果：
- `npm test` 通过。
- `node --check public\app.js` 通过。
- `node --check public\task\workflow.js` 通过。
- 重启真实端口 7439 后，新建 workflow 验证返回 2 个默认节点，类型为 `start` 和 `end`。
- 已清理验证过程中生成的临时 workflow 文档。

剩余风险或阻塞项：
- 当前已渲染节点端口，但还没有实现节点之间连线编辑；本次需求仅要求端口展示，不影响交付。
