# 交付说明：创建子任务 Bug 调查

## 源任务文件
`msg-2026-04-08T15-38-04-371Z.md`

## 任务目标
调查"创建子任务"功能是否存在 bug。

## 发现
**根因**：运行中的 Node 服务器进程（PID 2944，启动于 23:12:56）加载的是不包含 `/api/workspaces/:workspace/tasks/subtask` 路由的旧版 `server.js`。
- `POST /api/workspaces/Alith/tasks/subtask` 返回 404 "Cannot POST"
- 前端代码（`openSubtaskModal` → `submitSubtask`）和路由定义（server.js:1286）本身均无代码缺陷
- Express 5.2.1 路由匹配机制经独立测试验证正常

## 修复动作
- 停止旧进程（PID 2944）并重新启动 `node server.js`
- 重启后 `POST /api/workspaces/Alith/tasks/subtask` 返回 `201 Created`，子任务正常写入 `sessions/`

## 验证结果
- ✅ 使用 HTTP 客户端直接调用 subtask 端点，返回 201 + 正确 JSON
- ✅ 测试产生的临时文件已清理

## 剩余风险
- 每次修改 server.js 后需要重启服务器才能生效（Node.js 不会热重载）
