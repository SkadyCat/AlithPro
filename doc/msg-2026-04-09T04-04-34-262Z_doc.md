# Task: Styled message modal + node-message binding + dblclick viewer

- **Source**: `msg-2026-04-09T04-04-34-262Z.md`
- **Goal**: Replace ugly `prompt()` with styled modal. Bind messages to nodes. Double-click shows messages, not rename.

## Changes

### `application/public/mindmap.js`
- Double-click handler calls `window.MindMap.onNodeOpen(node)` callback instead of `dmStartEdit`

### `application/public/app.js`
- `dmShowMsgModal()` — centered dark modal with textarea, Ctrl+Enter to send, Esc/click-outside to cancel
- `dmFetchNodeMessages()` — fetches message content from sessions/inprocess/processed buckets
- `dmShowNodeViewer()` — viewer modal showing all messages with status (待处理/处理中/已处理), content, and processing results (_doc.md)
- `dmBuildNodePath()` — helper to build ancestor path string
- `window.MindMap.onLeaveMessage` — uses styled modal, binds message fileName to `node.messages[]`, saves draft
- `window.MindMap.onNodeOpen` — if node has messages, shows viewer; otherwise opens compose modal
- `buildMindmapPreview()` — added earlier for card preview

### `application/public/styles.css`
- Full CSS for `.dm-msg-backdrop`, `.dm-msg-modal`, `.dm-msg-viewer`, `.dm-msg-header`, `.dm-msg-input`, `.dm-msg-footer`, `.dm-msg-btn`, `.dm-msg-list`, `.dm-msg-item`, `.dm-msg-doc`, etc.

## Verification
- `node --check` on both JS files — syntax OK
- Committed as `fef7524`, pushed to `main`

## Risks
- `dmFetchNodeMessages()` tries sessions → inprocess → processed sequentially; could be slow with many messages.
- F2 key still works for rename (added in previous task).
