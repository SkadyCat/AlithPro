# 交付说明：卡片显示导图名称

**源任务文件**: `msg-2026-04-08T17-34-31-517Z.md`  
**任务链路**: `…msg-2026-04-08T16-18-40-549Z.md`

## 任务目标

思维导图卡片应显示导图名称（根节点标题），而非原始 JSON 内容。双击打开编辑器（已有功能）。

## 所做改动

### app.js
- `extractDraftTitle()` 增加 JSON 解析逻辑：检测到 `{"nodes":…}` 内容时，提取根节点 `title` 并加 🧠 前缀显示
- 处理服务器截断的 JSON preview（`startsWith('{"nodes"')`），通过 regex 提取 title
- `buildCard()` 中 drafts 卡片：当 preview 为 JSON 时隐藏 preview 区域，只显示标题

## 验证结果

- `app.js` 语法检查通过

## 剩余风险

无
