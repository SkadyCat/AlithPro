# Task: Ctrl+LMB pan, plain LMB box-select

- **Source**: `msg-2026-04-09T03-58-00-828Z.md`
- **Goal**: Change mind map canvas interaction — Ctrl+left-click for panning, plain left-click drag for box selection of nodes.

## Changes

### `application/public/mindmap.js`
- `dmCanvasMouseDown()`: split into two branches:
  - **Ctrl/Meta + LMB**: pans the canvas (preserves original behavior)
  - **Plain LMB**: draws a dashed selection rectangle; selects all nodes whose bounding box intersects the rectangle in screen-space

### `application/public/styles.css`
- Added `.dm-select-box` class: dashed purple border, translucent fill, `pointer-events: none`, `z-index: 3`

## Verification
- `node --check mindmap.js` — syntax OK
- Committed as `e59a496`, pushed to `main`

## Risks
- Node bounding box uses hardcoded 140×28; if node size changes the hit-test may be slightly off.
