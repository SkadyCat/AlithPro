# Task: Side panel for selected node messages

- **Source**: `msg-2026-04-09T08-52-48-847Z.md`
- **Goal**: Add a side panel next to the mind map canvas showing the selected node's messages and processed document content.
- **任务链路**: msg-2026-04-08T15-28-46-133Z.md > ... > msg-2026-04-09T08-20-57-739Z.md

## 关联文档
- 📄 文档原文: `msg-2026-04-09T08-52-48-847Z.md`
- 📋 交付文档: `msg-2026-04-09T08-52-48-847Z_doc.md`

## Changes

### `application/public/mindmap.js`
- At end of `dmRender()`, calls `window.MindMap.onSelectionChange(node)` with the single selected node (or null)

### `application/public/index.html` + `dev.html`
- Added `#dm_side_panel` div inside `.draft-modal-body` next to canvas
- Contains header with close button and scrollable content area

### `application/public/styles.css`
- `.draft-modal-body` now `display:flex` to lay out canvas + panel side-by-side
- `.draft-mm-canvas` changed from `width:100%` to `flex:1;min-width:0`
- `.dm-side-panel` — 320px wide, slides in/out via `.dm-side-hidden` class
- Full set of `.dm-side-*` styles for messages, status badges, doc previews, empty states
- Responsive: 260px width on mobile

### `application/public/app.js`
- `dmUpdateSidePanel(node)` — fetches messages for selected node, renders in side panel
- `window.MindMap.onSelectionChange` — triggers panel update on every selection change
- `window.MindMap.refreshSidePanel()` — forces re-fetch (called after creating new message)
- Side panel shows: node path, message list (status + content + processing result), "+ 新留言" button

## Verification
- `node --check mindmap.js` ✅
- `node --check app.js` ✅
- Committed as `6312c55`, pushed to `main`

## Risks
- First render may flash "加载中…" briefly while messages are fetched
