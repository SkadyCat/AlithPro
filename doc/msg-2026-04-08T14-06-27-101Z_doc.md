# Delivery: msg-2026-04-08T14-06-27-101Z

**Source task**: inprocess/msg-2026-04-08T14-06-27-101Z.md  
**Task goal**: 创建根节点时，自动弹出填写默认任务的对话框

## Changes Made (`application/public/app.js`)

- 右键菜单"📍 放置根节点"操作：`addNode()` 返回新节点后立即调用 `openNodeDocViewer(n)`
- 行为与 `addChild` 一致：节点创建后立刻弹出绑定/填写对话框

```js
// 修改前
{ label: "📍 放置根节点", action: () => { selectedIds.clear(); addNode(cx, cy, null); } }

// 修改后
{ label: "📍 放置根节点", action: () => { selectedIds.clear(); const n = addNode(cx, cy, null); openNodeDocViewer(n); } }
```

## Verification
`node --check app.js` 通过。**Commit**: `b3eb14a` — pushed to main

## Notes
无其他风险；`openNodeDocViewer` 内含重复打开防守（overlay 存在则直接返回）。
