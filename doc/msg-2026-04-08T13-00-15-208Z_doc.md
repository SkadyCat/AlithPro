# Delivery Doc: msg-2026-04-08T13-00-15-208Z

**Source task**: inprocess/msg-2026-04-08T13-00-15-208Z.md  
**Node path**: 背包功能 → 预制配装 → lodout → 里面的所有控件(UInventorySlotWidgetBase) 的子类控件 → 控件的EItemStatu计算逻辑 → 重构ItemStatu的计算逻辑 → 编辑模式下 → DisableImg 默认隐藏 → BUG:点击编辑按钮，需要刷新一遍所有控件的 DisableImg 为隐藏  
**Task goal**: BUG fix — 点击编辑按钮进入编辑模式时，需要刷新所有 UInventorySlotWidgetBase 子控件的 DisableImg 为隐藏状态

## Analysis

### BUG Description
When clicking the edit button to enter edit mode, the `DisableImg` images on all `UInventorySlotWidgetBase` sub-controls should be hidden. Currently they remain visible (showing stale state from the object pool).

### Fix Required
In the edit button click handler, iterate all inventory slot sub-controls and set `DisableImg->SetVisibility(ESlateVisibility::Hidden)` (or `Collapsed`).

Pseudo-implementation:
```cpp
// In the edit button OnClicked handler (likely Blueprint or C++):
TArray<UUserWidget*> SlotWidgets;
GetWidgetsByClass(UInventorySlotWidgetBase::StaticClass(), SlotWidgets);
for (UUserWidget* Widget : SlotWidgets) {
    UInventorySlotWidgetBase* Slot = Cast<UInventorySlotWidgetBase>(Widget);
    if (Slot && Slot->DisableImg) {
        Slot->DisableImg->SetVisibility(ESlateVisibility::Hidden);
    }
}
```

## Status: BLOCKED

**Reason**: `UInventorySlotWidgetBase` and its `DisableImg` member are implemented in Blueprint `.uasset` binary files (not text-based source). The text-based script files (Lua/AngelScript) at `E:\MagicWorld\Content\Script\` and `E:\MagicWorld\Script\` contain no references to `DisableImg`, `UInventorySlotWidgetBase`, `EditMode`, or `Loadout`.

The fix must be applied in the Unreal Engine Blueprint editor directly.
