# Task: Right-click node context menu with delete

## Source task file
`msg-2026-04-09T03-24-37-010Z.md`

## Goal
Right-click on a node should show a context menu with at least a delete option.

## Changes made
None — this feature was **already implemented** in prior tasks:
- `mindmap.js` line ~157: each node has a `contextmenu` event listener calling `dmNodeCtx()`
- `dmNodeCtx()` (line ~322) shows a context menu with:
  - ✦ 添加子节点 (add child)
  - ✎ 编辑 (edit / rename)
  - ✕ 删除 (delete node + descendants)
- Context menu CSS (`.dm-ctx`, `.dm-ctx-item`) already styled

## Verification
- Code inspection confirms right-click → context menu → delete flow is intact.

## Remaining risks / blockers
None.
