# Task: Right-click 留言 (Leave Message) feature

- **Source**: `msg-2026-04-09T04-00-23-556Z.md`
- **Goal**: Add "留言" option to right-click context menu. Creates a session task document with tree info, selected node, and user message.

## Changes

### `application/public/mindmap.js`
- Added "📝 留言" to `dmNodeCtx` context menu items
- Action calls `window.MindMap.onLeaveMessage(node, treeMarkdown)` callback

### `application/public/app.js`
- Registered `window.MindMap.onLeaveMessage` callback after MindMap setup
- Callback: prompts user for message, builds document with tree structure + node path + message, POSTs to `/api/workspaces/{ws}/tasks`
- Shows toast and refreshes workspace on success

## Document format
```
# 思维导图结构
[tree markdown]

# 当前选中节点
- 标题: {title}
- 路径: Root > Parent > Node

基于当前选中的节点和节点内容，你需要做: {user message}

你需要从这个链路去收束你的思考范围。
```

## Verification
- `node --check` on both files — syntax OK
- Committed as `c3cd8d1`, pushed to `main`

## Risks
- Uses browser `prompt()` for input — functional but basic UI.
