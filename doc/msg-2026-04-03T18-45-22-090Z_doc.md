源任务文件：`msg-2026-04-03T18-45-22-090Z.md`

任务目标：
- 排查并修复“其它 workspace 的筛选功能坏掉了”的问题。

所做改动：
- 更新 `application/public/app.js`，将时间筛选状态从全局共享改为按 workspace 隔离存储。
- 在切换 workspace 时恢复该 workspace 自己的筛选状态；没有历史状态时默认回到“全部”。
- 保留当前 bucket 级筛选逻辑不变，仅修复 workspace 之间互相串值的问题。

验证结果：
- `node --check public\app.js` 通过。
- `npm test` 通过。

剩余风险或阻塞项：
- 当前未引入持久化存储，刷新页面后每个 workspace 的筛选仍会回到默认“全部”；这是当前实现的预期行为，不影响本次串值修复。
