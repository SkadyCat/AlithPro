# Task: Add grid background + center position indicator to mind map

## Source task file
`msg-2026-04-09T03-15-18-002Z.md`

## Goal
Add a grid background that moves with pan/zoom and display the current viewport center coordinates, defaulting to (0, 0).

## Changes made
- **`application/public/mindmap.js`**:
  - `dmRender()` now updates canvas `backgroundSize` and `backgroundPosition` to make the dot grid follow pan and zoom.
  - Added `dmEnsurePosIndicator()` — creates a position overlay element once.
  - Position indicator computes viewport center in scene coordinates: `(-panX + width/2) / zoom, (-panY + height/2) / zoom` and displays as rounded integers.
- **`application/public/styles.css`**:
  - Added `.dm-pos-indicator` — monospace text positioned bottom-right, semi-transparent purple on dark background.

## Verification
- `node --check mindmap.js` — syntax OK.

## Remaining risks / blockers
None.
