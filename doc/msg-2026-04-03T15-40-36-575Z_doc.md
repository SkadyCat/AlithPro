源任务文件：`msg-2026-04-03T15-40-36-575Z.md`

任务目标：
- 为 Alith 所有卡片槽新增“最近、昨天、前3天、近一周”的时间筛选能力。

所做改动：
- 更新 `application/public/index.html` 与 `application/public/dev.html`，在草稿、待处理、处理中、完成处理四个卡片槽头部新增统一时间筛选下拉框。
- 更新 `application/public/app.js`，新增每个 bucket 的筛选状态，基于现有 `updatedAt` 字段做前端本地过滤，并在启用筛选时将槽位计数显示为“筛选后/总数”。
- 更新 `application/public/styles.css`，补充筛选控件与移动端布局样式，保证草稿按钮与筛选控件可同时展示。

验证结果：
- `application` 下执行 `npm test` 通过。
- `node --check public\app.js` 通过。
- 临时启动本地服务并抓取 `/` 与 `/dev`，确认两个页面都返回 4 个 `data-bucket-filter` 筛选控件。

剩余风险或阻塞项：
- 时间筛选基于文件 `updatedAt`（mtime）而非独立创建时间；如果后续存在批量改写文件的流程，筛选结果会随文件修改时间变化。
