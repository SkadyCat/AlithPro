# 交付说明

- 源任务文件: msg-2026-03-28T12-16-45-164Z.md
- 任务目标: 回答并修复“新建 workspace 是否还会继承 run.bat 工作目录问题”。

## 所做改动

- 修改 `application/server.js` 的 workspace 创建逻辑。
- 在 `POST /api/workspaces` 中，克隆模板并清空运行目录后，新增 `syncWorkspaceLaunchers(targetDir, templateModel)`，强制为新 workspace 重写 `run.bat` 和 `export_log.bat`。
- 读取模板当前 launcher 模型（`templateModel`），确保新建 workspace 继承模板的当前模型，同时使用新的 launcher 内容生成逻辑。
- 修改 `application/workspace/test/run.bat`，补上 `cd /d %~dp0`，让模板自身也与新规则一致。

## 结论

- 现在新创建的 workspace 不会再默认继承这个问题。
- 原因是创建流程在复制 `test` 模板后，会立刻用新的生成器重写 launcher，而新的生成器已包含 `cd /d %~dp0`。
- 已经创建好的旧 workspace 不会被这次逻辑自动回写；它们只有在手动修改或通过后续模型保存触发重写时，才会拿到新 launcher。

## 验证结果

- `npm test` 通过（`node --check server.js`）。
- 已检查 `application/server.js`，确认创建 workspace 后会调用 `syncWorkspaceLaunchers(...)`。
- 已检查 `application/workspace/test/run.bat`，确认模板 launcher 已包含 `cd /d %~dp0`。

## 剩余风险或阻塞项

- 之前已经存在的其它旧 workspace 若尚未重写 `run.bat`，仍可能保留旧行为。
