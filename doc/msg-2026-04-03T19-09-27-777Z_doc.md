源任务文件：`msg-2026-04-03T19-09-27-777Z.md`

任务目标：
- 草稿卡片右键新增“转换为任务流”。
- 将任务流逻辑独立封装到 `task` 文件夹。
- 点击任务流卡片弹出节点画布，可右键画布放置节点。
- 在 Alith 下创建 `WorkFlow` 目录管理 workflow 文档。

所做改动：
- 更新 `application/server.js`，新增 `WorkFlow` 目录支持、workflow 列表/创建/读取/保存/删除接口，以及从草稿一键转换为 workflow 文档的接口。
- 更新 `application/public/index.html` 与 `application/public/dev.html`，新增“任务流”卡片区、任务流统计项、节点画布弹窗，并加载独立脚本 `/task/workflow.js`。
- 更新 `application/public/styles.css`，新增任务流卡片、节点画布、节点巨型框、右键菜单和文档放置面板样式。
- 更新 `application/public/app.js`，接入 workflow 卡片渲染、草稿右键“转换为任务流”、任务流打开/保存/删除与新建任务流入口。
- 新增 `application/public/task/workflow.js`，将节点画布、右键菜单、节点拖拽、文档选择放置等任务流前端逻辑独立封装到 `task` 目录。
- 创建根工作区目录 `WorkFlow\`，用于管理 workflow 文档。

验证结果：
- `npm test` 通过。
- `node --check public\app.js` 通过。
- `node --check public\task\workflow.js` 通过。
- 本地启动临时服务后，完成了 workflow 冒烟链路：创建临时 workspace、创建草稿、草稿转换为 workflow、读取 workflow、保存节点更新，并确认首页返回任务流区/弹窗/脚本引用。

剩余风险或阻塞项：
- 任务流节点当前支持文档名展示、拖拽和保存坐标，但尚未加入连线、缩放或多选编辑能力；这些不影响本次所需的任务流基础画布。
