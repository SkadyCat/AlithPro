# Delivery Doc: msg-2026-04-08T11-59-26-240Z

**Source task**: inprocess/msg-2026-04-08T11-59-26-240Z.md  
**Parent task**: draft-2026-04-08T10-58-55-044Z.md  
**Task goal**: Click mind map node → popup dialog showing 留言文档 and Agent处理的文档

## Changes Made (commit `8c00d02`)

### server.js
- GET `/api/workspaces/:ws/item`: Now checks for `{stem}_doc.md` in `doc/` for **all** buckets (not just `processed`). Returns `linkedDocName` and `linkedDocContent` for any linked file.

### app.js (initMindMap)
- **�� badge click**: Root nodes with `linkedFile` now have a click listener on the badge → calls `openNodeDocViewer(n)`
- **Context menu**: Added "📖 查看文档" item (visible only when `n.linkedFile` is set)
- **`openNodeDocViewer(n)`** (new async function):
  - Creates a full-screen overlay with a two-tab dialog
  - Tab 1 "📝 留言文档": content of the linked file (the message/draft sent as task)
  - Tab 2 "🤖 Agent文档": `_doc.md` delivery note (or "暂无 Agent 处理文档" if none)
  - Fetches via existing `/api/workspaces/{ws}/item` endpoint
  - Close: ✕ button or click outside backdrop

### styles.css
- Added `.mm-doc-dialog`, `.mm-doc-header`, `.mm-doc-title`, `.mm-doc-close`, `.mm-doc-tabs`, `.mm-doc-tab`, `.mm-doc-body` styles

## Verification
- `node --check app.js && node --check server.js` passed
- Committed and pushed `8c00d02`
