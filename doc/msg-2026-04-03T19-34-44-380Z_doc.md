源任务文件：`msg-2026-04-03T19-34-44-380Z.md`

任务目标：
- 将所有卡片槽的默认时间筛选改为“最近”。

所做改动：
- 更新 `application/public/app.js` 中的 `createDefaultBucketFilters()`，将 `drafts / sessions / inprocess / processed` 四个卡片槽的默认筛选从 `all` 改为 `recent`。

验证结果：
- `node --check public\app.js` 通过。

剩余风险或阻塞项：
- 无。
