# 交付说明

- 源任务文件：$taskName
- 任务目标：在左侧 left_menu 的工作区项上新增右键删除 Workspace，在中间卡片上新增右键删除卡片与复制名称功能。

## 所做改动

1. 更新 pplication\server.js。
2. 新增 DELETE /api/workspaces/:workspace，用于删除普通工作区；同时保护根工作区 Alith 与模板工作区 	est 不可删除。
3. 新增 DELETE /api/workspaces/:workspace/item，用于删除卡片文件；删除 processed 卡片时会一并清理同名 _doc.md。
4. 更新 pplication\public\app.js，新增统一右键菜单、复制名称、删除 Workspace、删除卡片、删除后刷新与选中态回收逻辑。
5. 更新 pplication\public\styles.css，补充右键菜单样式与危险操作视觉状态。

## 验证结果

- 
ode --check server.js：通过。
- 
ode --check public\app.js：通过。
- 
pm test：通过（现有测试为 
ode --check server.js）。
- 在临时端口 7442 启动服务后，已完成链路验证：创建临时 Workspace、创建临时 sessions 卡片、删除该卡片、删除该 Workspace，且最终工作区列表中已不再存在该临时 Workspace。
- 在临时端口 7443 验证删除保护：删除 Alith 与 	est 均返回 400，确认保护生效。

## 剩余风险或阻塞项

- 当前“删除卡片”直接执行永久删除，没有回收站；这与本次需求一致，但操作不可撤销。
- dev 页面仍会显示右键菜单中的复制名称；由于其仅展示根工作区，删除项会保持禁用状态。
- 无其它阻塞项。
