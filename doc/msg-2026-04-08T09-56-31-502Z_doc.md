# 交付说明：center_area 一分为二 + 思维导图面板

**源任务文件：** `msg-2026-04-08T09-56-31-502Z.md`
**任务目标：** 将 center_area 左右分割，右侧新增思维导图，支持放置节点、Tab 派生子节点

## 改动

### `application/public/index.html`（其他 agent 完成）
- `center_area` 内添加 `#center_left` 包裹原有 5 个 bucket 列
- 新增 `#mindmap_panel` 右侧面板（工具栏 + `#mindmap_canvas` + `#mindmap_svg`）

### `application/public/styles.css`（其他 agent 完成）
- `.center-area { display:grid; grid-template-columns:1fr 420px }`
- `.center-left`、`.mindmap-panel`、`.mindmap-canvas`、`.mindmap-svg`、`.mm-node`、`.mm-node-input` 样式

### `application/public/app.js`（本次新增）
完整思维导图模块（IIFE `mm`）：

| 功能 | 实现 |
|------|------|
| 数据结构 | `nodes[]` 含 id/text/parentId/x/y |
| 添加根节点 | 双击空白区域 |
| Tab 派生子节点 | 选中节点后按 Tab |
| Enter 编辑 | 选中节点后按 Enter 或双击节点 |
| 拖拽移动 | 鼠标拖拽节点 |
| Delete 删除 | 删除节点及所有后代 |
| Escape 取消 | 退出编辑/取消选中 |
| SVG 连接线 | 贝塞尔曲线连接父子节点 |
| 清空按钮 | 清空所有节点 |
| 初始节点 | 启动时在画布中心放置"起始节点" |

## 验证

`node --check app.js` 通过
