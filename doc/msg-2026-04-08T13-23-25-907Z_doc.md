# Delivery Doc: msg-2026-04-08T13-23-25-907Z

**Source task**: inprocess/msg-2026-04-08T13-23-25-907Z.md  
**Task goal**: 单击不弹窗，只有双击才弹出文档查看器

## Fix (commit `2e20136`)

Removed `openNodeDocViewer(n)` call from `click` event listener. 

Before: click → opens doc viewer popup  
After: click → only `e.stopPropagation()` (selects node via mousedown, no popup)

`dblclick` still calls `openNodeDocViewer(n)` — double-click opens the viewer.

### Interaction summary after fix:
- **单击节点**: 选中节点（仅 mousedown 处理选中逻辑）
- **双击节点**: 弹出文档查看器（有 linkedFile）或创建任务绑定对话框（无 linkedFile）
- **Ctrl+单击**: 多选切换
- **右键**: 上下文菜单（重命名、添加子节点、留言、查看文档、删除）
- **拖拽**: 移动节点

## Verification
`node --check` passed. Pushed `2e20136`.
