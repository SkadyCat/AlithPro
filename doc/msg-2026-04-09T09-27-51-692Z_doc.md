# 交付文档 — dmToMarkdown 输出包含文档引用

## 源任务文件
`msg-2026-04-09T09-27-51-692Z.md`

## 节点路径
test > 日志逻辑 > 子节点测试2 > 测试生成文档 > 这是一个测试节点，测试父节点功能是否正常

## 任务目标
留言功能生成的 Markdown 树结构中，为有留言的节点追加 `[文档原文.md] [交付文档.md]` 引用标签。

## 改动内容
- **`application/public/mindmap.js`** — `dmToMarkdown()` 函数：遍历节点的 `messages[]` 数组，为每条留言生成 `[fileName] [fileName_doc.md]` 后缀，拼接在节点标题后。

### 改动前输出
```
- test
  - 日志逻辑
    - 子节点测试2
```

### 改动后输出
```
- test [msg-xxx.md] [msg-xxx_doc.md]
  - 日志逻辑 [msg-yyy.md] [msg-yyy_doc.md]
    - 子节点测试2
```

## 验证
- `node --check mindmap.js` 通过
- Committed `38362df`, pushed to `main`

## 关联文档
- 📄 文档原文: `processed/msg-2026-04-09T09-27-51-692Z.md`
- 📋 交付文档: `doc/msg-2026-04-09T09-27-51-692Z_doc.md`

## 任务链路
留言功能系列: 留言右键菜单 → 留言弹窗 → 节点绑定 → 侧面板 → 文档引用显示 → **树结构文档引用**

## 剩余风险
无
