# Task: Add default add/delete buttons to mind map nodes

## Source task file
`msg-2026-04-09T02-38-13-562Z.md`

## Goal
After creating child nodes via Tab, ensure:
1. Nodes are saved by default (already fixed in previous task)
2. Double-click to rename (already works)
3. Right-click context menu (already works)
4. Visible add (+) and delete (×) buttons on each node

## Changes made
- **`application/public/mindmap.js`** — `dmRender()`:
  - Each non-editing node now includes a `dm-node-actions` div with "+" (add child) and "×" (delete) buttons.
  - "+" button creates a child node with auto-edit mode (enters rename immediately).
  - "×" button selects the node and calls `dmDeleteSelected()`.
- **`application/public/styles.css`**:
  - Added `.dm-node-actions` — positioned top-right, hidden by default, shown on hover.
  - Added `.dm-node-btn` — small 20px circular buttons.
  - `.dm-node-add` — green tint for add button.
  - `.dm-node-del` — pink tint for delete button.

## Verification
- `node --check mindmap.js` — syntax OK.

## Remaining risks / blockers
None.
