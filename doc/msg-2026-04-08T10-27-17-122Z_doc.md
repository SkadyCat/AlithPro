# Delivery Doc: msg-2026-04-08T10-27-17-122Z

**Source task**: inprocess/msg-2026-04-08T10-27-17-122Z.md  
**Task chain**: ...> msg-2026-04-08T08-35-19-828Z.md > (this)  
**Task goal**: Placing a root node creates a 任务链路区 with a root document; any node right-click allows 留言 as a sub-chain document

## Changes Made

The other agent had already implemented the core in `initMindMap()`:
- `syncRootNodeToTask(n)`: when a root node's text is committed (`stopEdit`), calls `POST /api/workspaces/{ws}/drafts` to create a draft document; stores `linkedFile` + `linkedBucket` on the node; calls `setChainAlias` to name the chain after the node text
- `addMessageToNode(n)`: prompts for message text, calls `POST /api/workspaces/{ws}/tasks/subtask` linked to the root document

**This agent added/fixed:**
1. Extended "💬 留言" to all nodes (was root-only) — `addMessageToNode` now walks up to chain root to find `linkedFile`
2. Removed `if (n.parentId == null)` gate on "留言" menu item

## Verification

- `node --check app.js` passed
- Committed `dd50cb8` and pushed

## Behavior

- Place root node → name it → on blur/enter, a draft document is created in sessions with same title
- Chain alias set to node text in the UI
- Right-click any node → "💬 留言" → prompt → subtask created linked to the root chain doc

## Risks / Blockers

- `留言` on a non-root node that has no root with `linkedFile` will show toast error "节点尚未关联文档"
- Root doc is created as a **draft**, not a task in sessions; the agent won't auto-process it unless it's moved to sessions
