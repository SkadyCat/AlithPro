# Delivery Doc: msg-2026-04-08T12-49-21-821Z

**Source task**: inprocess/msg-2026-04-08T12-49-21-821Z.md  
**Task goal**: 按 Tab 创建的子节点默认往下生长（top-to-bottom 布局）

## Change (commit `f0fc238`)

**addChild (app.js)**:
```js
// Before
const x = parent.x + 160;          // tree grew to the RIGHT
const y = parent.y + siblings.length * 52;

// After
const x = parent.x + siblings.length * 160;  // siblings spread right
const y = parent.y + 80;                       // children grow DOWNWARD
```

- Children now appear 80px BELOW their parent
- Additional siblings spread 160px to the right
- edgePath draws parent-bottom → child-top cubic bezier — already correct for downward layout, no SVG changes needed

## Verification
- node --check passed
- Committed f0fc238; push pending (network outage)
