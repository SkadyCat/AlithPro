# Delivery: msg-2026-04-08T13-37-01-161Z

**Source task**: inprocess/msg-2026-04-08T13-37-01-161Z.md  
**Task goal**: 卡片应该显示部分文档内容（parent task链路的迭代子任务）

## Changes Made

Added `refreshNodeDocContent()` async function in `initMindMap` IIFE (`app.js`):

- **What it does**: For every node that has `linkedFile + linkedBucket` but no `docContent`, fetches the file content from the server (`/api/workspaces/:ws/item`) and populates:
  - `n.text = firstLine` (up to 60 chars) if node text is still placeholder
  - `n.docContent = remaining lines` (up to 300 chars) — shown in the `.mm-node-preview` div
- **Called at**: init after `load()+render()`, and after every workspace switch in `_mmSwitchWorkspace`
- All fetches run in parallel (`Promise.all`); a single `save()+render()` is triggered only if any node changed

**Commit**: `a7ec506` — pushed to main

## Verification
`node --check app.js` OK. On page load, nodes with linked files but missing docContent will automatically fetch and display preview text in the card body.

## Risks
- First render shows no preview; content appears after async fetch completes (typically <100ms)
- If file was deleted on server, fetch silently fails and node stays as-is
