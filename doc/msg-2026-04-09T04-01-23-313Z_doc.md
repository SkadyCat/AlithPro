# Task: F2 rename selected node

- **Source**: `msg-2026-04-09T04-01-23-313Z.md`
- **Goal**: Press F2 to rename the currently selected mind map node.

## Changes

### `application/public/mindmap.js`
- Added `F2` key handler in `dmKeyDown()`: when a single node is selected, pressing F2 calls `dmStartEdit(node)` to enter rename mode (same as double-click edit).

## Verification
- `node --check mindmap.js` — syntax OK
- Committed as `ca79fbf`, pushed to `main`

## Risks
None.
