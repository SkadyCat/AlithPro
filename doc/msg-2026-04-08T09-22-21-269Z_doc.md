# 交付说明 — msg-2026-04-08T09-22-21-269Z

**任务目标**：草稿和任务流卡片与完成处理的默认尺寸一致（不再大卡片）

## 根本原因

renderChainTree 中 isChainMember 判断使独立节点（无父无子）仍用 162px 全尺寸卡片，而草稿/任务流从不参与链路，永远是独立节点。

## 修复

- renderChainTree renderNode: 移除 isChainMember 判断，所有节点统一 compact=true
- renderProcessedGrouped standalones: 也改用 compact=true  
- styles.css: 移除 .card:not(.card-tree-node) 的 !important 锁定

效果：所有 bucket（草稿/待处理/处理中/完成处理/任务流）的全部卡片均使用紧凑树节点样式。

commit: HEAD，push 成功
