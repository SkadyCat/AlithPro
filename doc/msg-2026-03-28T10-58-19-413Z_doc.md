# 交付说明

- 源任务文件: msg-2026-03-28T10-58-19-413Z.md
- 任务目标: 说明 .active-workspace 的作用。

## 所做改动

- 未修改仓库代码。
- 查阅 `.github/copilot-instructions.md` 与 `.github/scripts/wait-for-session-doc.ps1` 中的工作区解析逻辑。
- 确认 `.active-workspace` 是仓库根目录下的文本文件，用于声明当前激活工作区名称。

## 结论

- `.active-workspace` 保存当前激活的工作区名称，例如 `test`。
- 读取到该文件后，代理与等待脚本会把目录切换到 `workspace/{name}/sessions`、`workspace/{name}/inprocess`、`workspace/{name}/processed`、`workspace/{name}/doc`、`workspace/{name}/logs`。
- 如果该文件不存在，则回退到仓库根目录下的旧版结构：`sessions`、`inprocess`、`processed`、`doc`、`logs`。
- 在等待脚本中，优先级是：显式参数 > `.active-workspace` 文件 > 根目录旧结构。

## 验证结果

- 已从 `.github/copilot-instructions.md:12-20` 验证工作区约定。
- 已从 `.github/scripts/wait-for-session-doc.ps1:21-33` 验证解析与回退逻辑。

## 剩余风险或阻塞项

- 无。
