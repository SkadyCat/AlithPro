# 交付说明：processed 列表分区显示

**源任务文件：** `msg-2026-04-08T08-09-38-664Z.md`  
**任务目标：** 相同任务链放同组；只有单独一个成员的任务链放到完成处理的第一行（顶部）

## 改动

**`application/public/app.js`** — `renderProcessedGrouped()`

重构渲染逻辑：

1. **顶部区：独立卡片（1 成员链）** — 按时间顺序排列，置于列表最前
2. **下方区：多成员链** — 每条链按最早成员时间排序，链内成员聚合显示；链 >2 成员时显示标题行

```
[完成处理]
  单独卡片A (时间最早)
  单独卡片B
  单独卡片C ...
  ─── 链标题（如链>2）───
  链成员1, 链成员2, 链成员3 ...
  链成员1, 链成员2 (无标题，链=2)
```

## 验证

- `node --check` 语法通过
- standalones 先按 `items.indexOf` 排序后渲染
- chains 按最早成员 `items.indexOf` 排序，链内成员也按时间顺序排列

## 风险/阻塞

无
