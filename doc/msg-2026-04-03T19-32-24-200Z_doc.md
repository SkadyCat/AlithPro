源任务文件：`msg-2026-04-03T19-32-24-200Z.md`

任务目标：
- 排查并修复点击“转换任务流”时请求失败的问题。

所做改动：
- 本次未修改代码。
- 复现后确认：临时端口上的最新代码可以正常执行“草稿 -> 任务流”转换，失败根因是 7439 端口上仍在运行旧版服务实例，未包含 `/api/workspaces/:workspace/drafts/:file/workflow` 新接口。
- 使用 `application\runserver.bat` 重启 7439 上的 Alith 服务，使其切换到当前最新代码。

验证结果：
- 在临时端口 7445 上创建草稿并调用转换接口，成功返回 workflow 文件名。
- 在真实端口 7439 上重启后，再次执行“草稿 -> 任务流”转换，成功返回 workflow 文件名。
- 已清理验证过程中生成的临时草稿与 workflow 文档。

剩余风险或阻塞项：
- 无。
