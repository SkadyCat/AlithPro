# 交付说明 — msg-2026-04-08T07-41-48-006Z

**源任务文件**：msg-2026-04-08T07-41-48-006Z.md
**任务目标**：left_menu 右键 workspace → 克隆功能（含重命名、保留文档 checkbox、不允许同名）

## 结论

该功能已在前序版本中完整实现，无需修改。本次任务的作用是验证实现是否符合需求。

## 已实现内容

### 前端（app.js）

**右键菜单**（enderWorkspaces()，workspace btn contextmenu 事件）：
- 菜单项"克隆 Workspace"→ 调用 openCloneWorkspaceModal(ws.name)
- 另有"复制名称"和"删除 Workspace"菜单项

**克隆弹窗**（nsureCloneModalDOM / openCloneWorkspaceModal）：
- 动态创建 #clone_ws_modal（Modal Backdrop 模式）
- 新名称输入框，预填 {来源名称}-copy
- **"保留文档" checkbox**，默认勾选（checked=true）
- Enter 确认 / Escape 取消 / 点击遮罩取消

**提交逻辑**（submitCloneWorkspace）：
- 空名称校验 → toast error
- **同名校验**：新名 === 来源名 → toast error；服务端 409 → toast error
- POST /api/workspaces/:source/clone { newName, keepDocs }
- 成功后刷新 workspace list，toast 提示

### 后端（server.js）

**POST /api/workspaces/:workspace/clone**：
- 验证来源 workspace 存在
- nsureSafeWorkspaceName 校验新名称格式
- **不允许同名**：stat targetDir，已存在则 409
- s.cp(sourceDir, targetDir, { recursive: true }) 拷贝
- keepDocs=false：清空 drafts/sessions/doc/processed/logs/inprocess/WorkFlow
- keepDocs=true：保留所有内容原样
- syncWorkspaceLaunchers：同步 launcher 配置

## 验证

- node --check 通过
- 逻辑覆盖所有需求：重命名 ✅、保留文档 checkbox ✅、不允许同名 ✅

## 注意

若用户在远程服务器（43.139.72.98:7439）测试，git push 暂时无法从本机自动推送（网络受限），需在有外网访问的机器上手动执行 git push。
