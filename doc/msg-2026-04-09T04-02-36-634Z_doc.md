# Task: Mind map card size consistency

- **Source**: `msg-2026-04-09T04-02-36-634Z.md`
- **Goal**: Make mind map draft cards visually consistent with other cards in the workspace list.

## Changes

### `application/public/app.js`
- Added `isMindMapDraft` detection (checks for `{"nodes"` prefix in preview)
- Mind map drafts get `.card-mindmap` class instead of `.card-draft`
- Added `buildMindmapPreview()` — shows node count + first 4 node titles as preview text
- Preview now shown in both compact (tree) and standard card layouts

### `application/public/styles.css`
- Added `.card.card-mindmap` — purple accent border matching mind map theme
- Added `.card.card-mindmap::after` — "思维导图" badge (purple) instead of "Markdown" (blue)

## Verification
- `node --check` on both files — syntax OK
- Committed as `48c09be`, pushed to `main`

## Risks
- `buildMindmapPreview` depends on full JSON being available in `item.preview`; truncated previews may fail silently (returns empty string).
