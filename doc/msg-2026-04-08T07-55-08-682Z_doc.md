# 交付说明 — msg-2026-04-08T07-55-08-682Z

**源任务文件**：msg-2026-04-08T07-55-08-682Z.md
**任务目标**：processed 区域只有任务链成员 >2 时才显示分组标题行；独立子任务直接列于下方

## 改动

**app.js** enderProcessedGrouped()：

`diff
- if (groupItems.length > 1 || groupItems[0].subtaskParent) {
+ if (groupItems.length > 2) {
`

## 行为变化

| 场景 | 改动前 | 改动后 |
|------|--------|--------|
| 独立卡片（无 subtaskParent，无子任务） | 无标题 | 无标题（不变） |
| 1 个子任务（仅 1 个成员有 subtaskParent，根不在 processed） | **显示标题** | 无标题 ✅ |
| 根 + 1 个子任务（2 成员） | **显示标题** | 无标题 ✅ |
| 根 + 2 个或以上子任务（≥3 成员） | 显示标题 | 显示标题（不变） |

## 验证

- node --check 通过
