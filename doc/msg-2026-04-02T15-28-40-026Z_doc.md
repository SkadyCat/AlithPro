# 交付说明

- 源任务文件: `msg-2026-04-02T15-28-40-026Z.md`
- 任务目标: 修复所有 workspace 的等待恢复问题说明。
- 所做改动:
  - 统一更新根仓库与所有现有 workspace 的 `.github\copilot-instructions.md`。
  - 在等待流程说明中新增规则：任何 wait 完成后都必须立即重扫 `sessions/`，并把该队列状态作为恢复处理的唯一事实来源。
  - 明确 `read_powershell` 只能作为辅助日志信号，不能作为恢复队列的唯一条件。
  - 同步修复批量替换时引入的 Markdown 格式问题，确保 `sessions/` 与 `read_powershell` 代码样式正确。
- 验证结果:
  - 已抽查根仓库、`test`、`UIEditor` 的说明文件内容。
  - 已运行 `application` 下现有校验命令 `npm test`，通过。
- 剩余风险或阻塞项:
  - 该修复作用于执行说明层；若某个外部执行者完全忽略说明，仍可能自行实现出脆弱恢复逻辑。
