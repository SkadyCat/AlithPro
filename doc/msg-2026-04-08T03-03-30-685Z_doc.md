# msg-2026-04-08T03-03-30-685Z delivery note

- Source: msg-2026-04-08T03-03-30-685Z.md
- Goal: Analyze why Start Agent button fails while run_without_proxy.bat works directly

## Root Cause
1. Proxy port 7897 is NOT running on this machine
2. run.bat sets HTTP_PROXY/HTTPS_PROXY to 127.0.0.1:7897 — Copilot CLI fails to connect
3. run_without_proxy.bat has no proxy env vars — works fine
4. Server was started at 10:37 (before code changes at 10:44) — old endpoint always used run.bat

## Fix Applied
- Restarted server (runserver.bat) to load new code
- New server now accepts useProxy param from frontend checkbox
- When proxy checkbox is unchecked, run-agent spawns run_without_proxy.bat instead of run.bat
- API now returns useProxy field confirming correct routing

## Verification
- POST /api/workspaces/test/run-agent with useProxy:false returns {started:true, useProxy:false}
- Server running new code (confirmed by useProxy field in response)

## Blockers
- None
