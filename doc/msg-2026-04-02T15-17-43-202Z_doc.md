# 交付说明

- 源任务文件: `msg-2026-04-02T15-17-43-202Z.md`
- 任务目标: 分析 agent 处理完 2 个文档后挂起、无反应的原因。
- 分析结论:
  - `wait-for-session-doc.bat` 本身逻辑正常：它持续等待 `sessions\*.md`，一旦发现文件就输出 `TASK_DETECTED <file>` 并退出。
  - 真正的不稳定点在等待结果的读取链路：agent 将 `wait-for-session-doc.bat 30 continuous` 作为异步 shell 运行，完成后依赖 `read_powershell(shellId)` 读取结果。
  - 本轮运行中已复现一次：系统通知 wait 命令已完成，但随后读取返回 `Invalid shell ID: wait-session`。这说明 shell 在完成后有时已被运行时清理，导致检测结果丢失。
  - 如果 agent 严格依赖这次读取结果来恢复处理，而没有在通知后立即重新扫描 `sessions\`，就会表现为“处理完两份后挂起，没有任何反应”。
  - 次要表现因素是等待态本身没有持续输出；即使 agent 还在等，新任务未被重新扫描前，外部看起来也像静默挂起。
- 证据:
  - `wait-for-session-doc.bat:10-23` 显示脚本只做扫描、`timeout`、再循环，发现任务即退出。
  - 本轮运行中第一次 wait 返回后，读取结果出现 `Invalid shell ID: wait-session`。
  - 第二次 wait 返回后，`read_powershell` 成功读到 `TASK_DETECTED msg-2026-04-02T15-17-43-202Z.md`，说明问题是读取通道不稳定，不是脚本不产出结果。
- 建议:
  - 收到 wait 完成通知后，不要仅依赖 `read_powershell`；应始终立即重新扫描 `sessions\` 作为权威恢复路径。
  - 可把 `read_powershell` 仅作为辅助日志来源，而不是恢复队列的唯一条件。
- 验证结果:
  - 已核对 `wait-for-session-doc.bat` 源码。
  - 已核对 `logs\agent-realtime.log` 中历史等待/恢复轨迹，并结合本轮 wait 行为完成归因。
- 剩余风险或阻塞项:
  - 根因在运行时等待结果消费方式，不在仓库业务代码；若后续执行者仍只依赖 `read_powershell`，现象可能再次出现。
