# 交付说明 — 实施 v5 留言模板格式

## 源任务文件
`msg-2026-04-10T09-38-36-682Z.md`

## 任务目标
将经过 5 轮迭代确认的留言模板格式同步到 `app.js` 代码中。

## 所做改动

### 1. 内容模板重构 (`app.js` ~line 728-745)

**旧模板**:
```
{空间树}
我们目前需要解决的是: {message}
你在解决这个问题之前，你已经具备了以下知识：
{知识大纲}
你需要从这个链路去收束你的思考范围。
```

**新模板**:
```markdown
# 行为树留言上下文

## 项目结构（空间树）
{空间树}

## 当前任务
**工作内容**: {message}
| 属性 | 值 |
| 节点ID | {id} |
| 节点名称 | {title} |
| 节点类型 | {type} |
| 执行路径 | A → B → C |

## 已有知识（行为树执行上下文）
> Seq/Sel 裁剪规则说明
{知识大纲}

## 约束
基于以上行为树执行链路收束思考范围。
```

### 2. 文档引用标签缩短 (`nodeText()` ~line 579-587)
- `需求文档` → `需求`
- `交付文档` → `交付`

### 3. 新增元数据
- `nodeType`: 从 `_dm.nodeTypes[node.type].label` 获取
- `pathStr`: 复用 `dmBuildNodePath(node)` 并将 ` > ` 替换为 ` → `

## 验证结果
✅ 代码语法正确，模板结构完整
- 需浏览器刷新后在下次留言中验证实际输出

## 剩余风险
- 首次留言前需刷新浏览器加载新版 app.js
