# 交付说明 — 按空间顺序dump树结构到留言开头

## 源任务文件
`msg-2026-04-10T07-14-21-198Z.md`

## 任务目标
按照节点的空间顺序（y坐标排序）将完整树结构dump下来，附带节点ID，放到留言内容的开头。

## 所做改动

### `application/public/app.js`
1. **新增 `dmBuildSpatialTree()` 函数**（插入在 `dmBuildNodePath` 之前）：
   - 从根节点遍历整棵树
   - 每层子节点按 y 坐标排序（空间从上到下）
   - 输出格式：缩进树 + `[id][Type] title`
2. **更新消息内容模板**：在 `onLeaveMessage` 中，将 `dmBuildSpatialTree()` 输出放到 content 最开头

### 输出示例
```
- [1] 测试
  - [2][Seq] 友好值系统
    - [3][Seq] 定义一些基础设定
      - [4][Action] 定义AccountID
      - [5][Action] 定义 NetID
      - [18][Action] Password
      ...
    - [6][Seq] 事件
      ...
    - [11][Sel] 逻辑
      ...
```

## 验证结果
✅ Node.js 测试确认：树按 y 坐标排序、ID 正确、层级缩进正确

## 剩余风险
- 浏览器需刷新才能加载新版 app.js
