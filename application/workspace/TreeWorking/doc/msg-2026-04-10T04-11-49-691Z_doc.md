# Delivery Note: BUG — 执行链路异常

## Source Task
- File: `msg-2026-04-10T04-11-49-691Z.md`
- Path: 测试 > 友好值系统 > 事件 > BUG: 执行链路异常

## Objective
Fix two issues with `dmBuildExecutionChain()`:
1. Output should be a **flat linear chain** (no indentation/subtree), so the agent sees a clear execution order.
2. **Sel nodes** should only expand the branch relevant to the current node; other branches are omitted.

## Changes Made

### `application/public/app.js` — `dmBuildExecutionChain()`
- Removed indentation (`"  ".repeat(indent)`) — all items are now `- label` at the same level.
- Changed `walk(node, indent)` signature to `walk(node)` (no indent param).
- **Seq / root / other nodes**: depth-first traversal expands ALL children in order (all execute in a sequence).
- **Sel nodes**: only the child on the ancestor path to the target is expanded; other branches are skipped.
- Previously, only siblings of path-ancestors were shown as leaves (non-expanded). Now the full subtree under each Seq is flattened.

### `drafts/draft-2026-04-10T03-21-14-013Z.md`
- Set node 10 `status: "done"`.

## Validation
Tested with Node.js against the live draft:

**BUG node (id 10) chain:**
```
- 测试
- [Seq] 友好值系统
- [Seq] 定义一些基础设定
- [Action] 定义AccountID ✅
- [Action] 定义 NetID
- [Seq] 事件
- [Action] 重构... ✅
- [Action] 测试 新留言文档 ✅
- [Action] BUG: 执行链路异常。 ← 当前节点
```

All Seq children are expanded. Flat linear format confirmed.

## Remaining Risks / Blockers
- Sel-node logic is implemented but untested in production (no Sel nodes exist in the current draft). Will be validated when a Sel node is added.
