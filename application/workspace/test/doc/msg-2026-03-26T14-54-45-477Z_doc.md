Source task file: `msg-2026-03-26T14-54-45-477Z.md`

Objective:
- Create `export_log.bat` so running it writes shell output to a log file instead of showing it in the shell.

Changes made:
- Added `export_log.bat` in the workspace root.
- Matched the existing `run.bat` launch settings:
  - `--allow-all`
  - `--model=gpt-5.4`
  - `--log-dir=logs`
  - `--log-level=all`
- Redirected all launcher output to `logs\copilot-shell.log`.
- Passed through extra CLI arguments with `%*` so the launcher can be reused for validation and normal startup.
- Simplified the log filename to a stable path after a timestamp-based variant proved unreliable on this Windows environment.

Validation results:
- Ran `export_log.bat --version`.
- Confirmed output was written to `logs\copilot-shell.log`.
- Confirmed the log contains the Copilot CLI version output.

Remaining risks or blockers:
- `logs\copilot-shell.log` is overwritten on each run. This matches the current implementation but may be worth changing later if log history is needed.
