# Delivery Note

- Source task file: msg-2026-03-26T16-23-25-811Z.md
- Objective: Make it obvious which models can be selected in the dashboard.

## Changes made
- Updated `G:\GameExPro3\AILab\public\index.html` to show an “Available models” area next to the model input.
- Updated `G:\GameExPro3\AILab\public\styles.css` to style visible preset chips in the dark dashboard theme.
- Updated `G:\GameExPro3\AILab\public\app.js` so the preset list renders from backend settings and each chip can fill the model input with one click.

## Validation results
- `npm test` -> passed.
- `node --check .\\public\\app.js` -> passed.

## Remaining risks or blockers
- The dashboard shows the currently configured preset list; if more models are added later, they should also be added to the backend preset source.
