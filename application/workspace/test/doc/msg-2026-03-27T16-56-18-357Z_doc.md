Source task file: msg-2026-03-27T16-56-18-357Z.md Objective: Make the top-right model switch interaction rewrite run.bat immediately when clicked, instead of only changing the UI state.  Changes made: - Updated `G:\GameExPro3\AILab\public\app.js` so clicking a model preset chip immediately saves the model through `/api/workspaces/:workspace/settings`. - Added `persistModelSelection()` in `public\app.js` to centralize model persistence, summary refresh, and user feedback. - Wired the top-right model `<select>` change event to immediately persist the selected model and trigger the existing backend launcher rewrite. - Kept the Save Model button working by routing it through the same immediate persistence path.  Validation results: - `npm test` succeeded (`node --check server.js`). - `node --check G:\GameExPro3\AILab\public\app.js` succeeded. - Verified `public\app.js` now calls `persistModelSelection()` from both model chip clicks and selector changes.  Remaining risks or blockers: - No automated browser interaction test exists here, so the UI behavior was validated by code path inspection and JavaScript syntax checks rather than a headless click test.
