Source task file: msg-2026-03-27T17-01-03-052Z.md Objective: Stop the top-right model switch from writing config.json and make it rewrite run.bat directly instead.  Changes made: - Updated `G:\GameExPro3\AILab\server.js` so `/api/workspaces/:workspace/settings` rewrites `run.bat` and `export_log.bat` directly without calling `writeWorkspaceConfig(...)`. - Updated `G:\GameExPro3\AILab\server.js` so `/api/workspaces/:workspace/run-agent` also syncs launcher files directly instead of persisting the model into `config.json` first. - Added `readRunBatModel()` and switched workspace summary settings to read the current model from `run.bat`, so the UI reflects the launcher file rather than `config.json`. - Left `G:\GameExPro3\AILab\workspace\test\config.json` untouched; its old `model` value no longer drives the top-right model switch flow.  Validation results: - `npm test` succeeded (`node --check server.js`). - `node --check G:\GameExPro3\AILab\public\app.js` succeeded. - Verified the settings endpoint now calls `syncWorkspaceLaunchers(...)` directly and does not write `config.json` in its body. - Verified `run.bat` still contains the active launcher model while `config.json` remains unchanged, proving the UI source of truth is now the batch file path.  Remaining risks or blockers: - `writeWorkspaceConfig()` still exists for older code paths, but the top-right model switch and run-agent flow no longer use it.
