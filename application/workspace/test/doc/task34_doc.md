Source task file: `task34.md`

Objective: Create a `runserver.bat` file to run the dashboard server.

Changes made:
- Added `G:\GameExPro3\AILab\runserver.bat`.
- Configured the launcher to switch to the app root and execute `npm start`.

Validation results:
- Started `runserver.bat` and confirmed it launched the dashboard server at `http://localhost:3000`.
- Stopped the temporary validation instance after the startup check.

Remaining risks or blockers:
- None.
