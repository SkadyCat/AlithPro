# 交付说明 — 环形分裂逻辑确认

## 源任务文件
`msg-2026-04-06T12-56-28-801Z.md`

## 目标
确认环形分裂逻辑实现与预期一致："弹道创建以后，会向四个方向发射这个弹道的复制体"

## 当前实现流程
1. **Lua skill_comp.lua** 创建子弹，应用被动链
2. **StraightShot.process()** → 设置子弹速度（dir * 100）
3. **RingSplit.process()** → `bullet:AddBehavior(BulletRingBehavior)` 添加环形行为
   - ProjectileCount=4, Radius=200, TriggerDistance=1
4. **Bullet.as Tick()** → `BulletRingBehavior.TickBehavior()` 每帧累计飞行距离
5. 距离 ≥ 1 → **DoRingRelease()** 触发：
   - 在子弹当前位置为中心，半径200的圆周上生成4个等分投射物
   - 每个投射物方向 = 圆心指向圆周点的方向（0°/90°/180°/270°）
   - 技能链继承原弹道（过滤掉环形分裂被动，避免递归）
   - 通过 AS `SkillManager.DoSkillByCustomArray()` 创建（走 AS 执行路径）
6. 原弹道销毁

## 与预期行为的匹配
✅ "弹道创建以后" → TriggerDistance=1，几乎立即触发
✅ "向四个方向" → ProjectileCount=4，均匀分布 360°
✅ "发射这个弹道的复制体" → 继承原技能链（含直射被动），复制体也会直线飞行

## 依赖修复
- Task 34：MagicStraitBall.process() — 子弹速度设置
- Task 35：MagicProjectileRing.process() — 环形行为添加
- Task 36：skillLinks 深拷贝 — 确保 DoRingRelease 能读取技能链

## 风险
- Task 36 的 `TArray:Add()` 深拷贝需运行时验证
- 环形投射物通过 AS SkillManager 创建，AS 执行路径需保持可用
