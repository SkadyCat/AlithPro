# 交付说明：msg-2026-04-06T07-45-46-271Z

## 源任务
`msg-2026-04-06T07-45-46-271Z.md` — 去掉 key 和 assetName，按 Primary/Passive 拆分为两个 Lua 文件

## 目标
将 SkillNodeData.lua 拆分为 PrimaryNodeData.lua 和 PassiveNodeData.lua，去掉 table key 和 assetName 字段。

## 完成内容
- 更新 `application/tools/json_to_lua.js`：按 `releaseType` 数组是否包含 `"primary"` 拆分，使用数组风格 table（无 key），移除 `assetName` 字段
- 生成 `Script/com/ability/node/PrimaryNodeData.lua`（3 个主动技能，5,988 bytes）
  - DA_PrimaryAbility_WaterBall, DA_PrimaryAbility_Thunder, DA_PrimaryAbility_FireBall
- 生成 `Script/com/ability/node/PassiveNodeData.lua`（14 个被动/配置，20,013 bytes）
  - DA_Accelerate, DA_Bounce, DA_Bounce_Exp, DA_BounceScatter, BallStrait, BallFollow, DA_Passive_FireExp, DA_ApearEnemyTop, DA_Ring, DA_Split, DA_MagicPassiveTracking, DA_Ability_Default, AbilityConf, PrimarySkillConf
- 删除旧的 `SkillNodeData.lua`

## 验证
- ✅ Primary 3 个 + Passive 14 个 = 17 个（与原合并文件一致）
- ✅ 无 assetName 字段，无字符串 key 索引
- ✅ 注释中保留了资产名用于参考

## 风险
无
