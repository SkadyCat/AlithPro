# 交付说明：导出 DA_Accelerate 到 JSON

## 源任务
`msg-2026-04-06T06-53-42-920Z.md` — 将 DA_Accelerate 导出为 JSON 并告知路径

## 导出结果

**JSON 文件路径：**
```
G:\GameExPro3\AlithEx\Alith\application\workspace\Skills\application\exports\DA_Accelerate.json
```

**内容包含：**
- AssetClass: `/Script/Angelscript.MagicProjectileAccelerate`
- ContentPath: `/Game/DA/Ability/DA_Accelerate.DA_Accelerate`
- ClassHierarchy: UMagicProjectileAccelerate → UMagicPassive → UMagicAbility → UAsAsset → UMagicAsset
- 9 个属性（含继承）：AccelerationFactor(1.5f), AbilityType, ReleaseType, ElementTypes, SkillName, Desc, AssetID, Icon, CallWords

## 改动内容

### server.js
- 新增 `GET /api/export/:name` 端点：导出任意 DA 实例为 JSON，同时写入 `exports/` 目录
- 可通过 `http://localhost:3220/api/export/DA_Accelerate` 访问

### exports/DA_Accelerate.json
- 新生成的 JSON 导出文件

## 说明
- 当前导出基于 AngelScript 类定义推断属性和默认值
- 如需获取 UE 编辑器中的实际运行时属性值，需在 UE 中运行 ExportAllSubAssetsToJson() 后刷新

## 风险
无
