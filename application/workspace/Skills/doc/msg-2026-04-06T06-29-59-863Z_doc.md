# 交付说明：端口改 3220 + runskillserver.bat + 拉起服务

## 源任务文件
`msg-2026-04-06T06-29-59-863Z.md`

## 任务目标
将技能可视化服务端口改为 3220，创建 `runskillserver.bat` 启动脚本，并拉起服务。

## 所做改动
1. **`application/server.js`**：`const PORT = 3210` → `const PORT = 3220`
2. **`runskillserver.bat`**（新建）：`cd /d "%~dp0application" && node server.js`，双击即可启动
3. 以 detach 模式启动服务，持续运行在 `http://localhost:3220`

## 验证结果
- `GET http://localhost:3220/api/skills` → 200 OK，返回 153 个节点
- 服务已在后台持续运行

## 剩余风险或阻塞项
无
