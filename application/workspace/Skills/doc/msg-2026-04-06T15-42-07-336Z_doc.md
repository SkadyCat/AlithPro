# 交付说明 — projectile-design.html 导入 600 新技能

## 源任务
`msg-2026-04-06T15-42-07-336Z.md`

## 目标
将 600 个新生成技能（100 主技能 + 500 辅助石）的完整信息导入到 projectile-design.html 页面。

## 完成内容

### 生成器改动 (`tools/generate_skill_catalog.js`)
- 新增 `CATALOG_JSON` 输出路径，指向 `public/skill-catalog.json`
- 新增 `generateCatalogJSON()` 函数，导出 primaries/passives/elements/forms/categories 完整数据
- 运行后自动生成 ~150KB 的 JSON 目录文件

### 页面重写 (`public/projectile-design.html`)
- 从静态硬编码数据改为动态 `fetch('/skill-catalog.json')` 加载
- 5 个标签页：总览 | 主技能(100) | 辅助石(500) | 现有实现 | 组合链路
- **总览**：统计卡（600技能/10形态/10分类/10元素/11已实现）、设计原则、形态分布、辅助石分布
- **主技能**：按形态分组，支持搜索 + 形态筛选 + 元素筛选，分组可折叠
- **辅助石**：按分类分组，支持搜索 + 分类筛选，分组可折叠
- **现有实现**：保留原有 3 主技能 + 11 被动（含 Behavior 映射）
- **组合链路**：保留 8 个组合链路示例
- 元素/形态/分类均有对应图标和颜色

## 验证结果
- ✅ 页面 HTTP 200，25KB
- ✅ skill-catalog.json HTTP 200，154KB
- ✅ 搜索、筛选、折叠功能在 HTML 中正确实现
- ✅ 保留了所有原有手工策划内容

## 注意事项
- 重新运行生成器会自动更新 JSON 和页面内容
- 原有 expansion 提议数据已合并到生成目录中（不再单独显示）
