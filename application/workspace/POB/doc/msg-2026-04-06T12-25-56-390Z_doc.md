# 交付说明 — 修复伤害分析中缺失词条归因

## 源任务
`msg-2026-04-06T12-25-56-390Z.md`

## 目标
修复伤害分析页面中看不到道具和词缀引用的问题。

## 根因
在 Task 13 中重写了 `damage-breakdown.js`，新版本返回 `allMods` 数组用于词条归因展示。但通过侧栏加载旧版保存的会话时，缓存的 `damageBreakdown` 数据是旧格式（无 `allMods`），导致词条来源区域不渲染。

## 改动

### `application/public/js/app.js`
- `selectBuild()` 加载会话后，增加 `refreshDamageBreakdown()` 调用
- 确保即使加载旧格式的会话，也会从 API 重新获取包含 `allMods` 的最新分析数据
- 新解码 (`doDecode`) 路径不受影响（已直接调用新 API）

## 验证
- 服务器重启成功，3 个会话可正常加载
- 侧栏点击加载旧会话后，`refreshDamageBreakdown()` 自动请求新格式数据

## 风险
- 无
