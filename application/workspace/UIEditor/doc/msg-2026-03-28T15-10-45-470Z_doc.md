# 交付说明

- 源任务文件：msg-2026-03-28T15-10-45-470Z.md
- 任务目标：为当前 session 的 UI 背景资源提供导出能力，并提供可点击的按钮入口。

## 所做改动

- 更新 `application\canvas-editor\public\index.html`，在工具栏新增“导出背景”按钮。
- 更新 `application\canvas-editor\public\app.js`，抽取当前 session 资源扫描逻辑为复用函数。
- 新增 `exportSessionBackgroundAssets()`：扫描当前 session 所有 `bgImage` 引用，去重后导出。
- 导出行为会先生成一份 `*-background-assets.json` 清单，再逐个下载对应背景资源文件，便于追踪资源来源与引用控件。

## 验证结果

- `node --check public\app.js` 通过。
- 运行中的 `canvas-editor` 首页 `http://127.0.0.1:8331/` 返回 HTTP 200。

## 剩余风险或阻塞项

- 浏览器批量下载是否会被拦截，取决于具体浏览器的下载策略；导出逻辑本身已按顺序触发并保留清单文件。
- 无其他阻塞项。
