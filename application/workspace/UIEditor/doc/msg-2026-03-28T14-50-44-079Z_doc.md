# 交付说明

- 源任务文件: `msg-2026-03-28T14-50-44-079Z.md`
- 任务目标: 为 `canvas-editor` 设计一个“召唤物管理界面”。

## 所做改动

- 新增了 `canvas-editor` 会话文件：`application\canvas-editor\data\docs\sessions\summon_management.session`
- 按现有 `canvas-editor` 会话格式设计了一套完整的召唤物管理界面草图。

## 设计内容

- 顶部状态栏：显示页面标题、编队容量、碎片资源等总览信息。
- 左侧名册区：提供召唤物列表、筛选标签，以及当前选中卡片。
- 中央详情区：展示主召唤物的大图区域、基础信息、忠诚度、以及核心属性条。
- 右侧指令区：放置主动技能槽、培养/出战等核心操作按钮。
- 整体风格延续现有 `canvas-editor` 深色奇幻 UI 方向，适合作为 RPG / 幻兽系统管理页。

## 验证结果

- 新会话文件已成功写入：`application\canvas-editor\data\docs\sessions\summon_management.session`
- JSON 结构校验通过。
- 由于本地 `canvas-editor` 服务可用，已额外验证该文件可通过 `http://127.0.0.1:8331/docs/api/get?name=sessions/summon_management.session` 被读取。

## 剩余风险或阻塞项

- 当前无阻塞项。
- 本次交付为静态界面草图会话，尚未扩展为带 DataAsset 绑定或 TileView EntryClass 的完整数据驱动版本。
