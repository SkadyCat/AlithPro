# 交付说明

- 源任务文件：msg-2026-03-28T15-53-58-621Z.md
- 任务目标：将导出范围从仅 `bgImage` 调整为当前 session 中所有控件的背景图片资源。

## 所做改动

- 更新 `application\canvas-editor\public\app.js`，将导出筛选范围扩展为当前 session 里所有图片类资源引用，而不再只限制 `bgImage`。
- 导出清单改为 `*-control-image-assets.json`，并在清单中记录每个图片资源对应的控件、类型和引用位置。
- 更新 `application\canvas-editor\public\index.html` 中按钮提示文案，使其与新的导出范围一致。

## 验证结果

- `node --check public\app.js` 通过。
- 运行中的 `canvas-editor` 首页 `http://127.0.0.1:8331/` 返回 HTTP 200。

## 剩余风险或阻塞项

- 浏览器对批量下载的策略仍可能影响连续下载体验，但导出逻辑与资源清单已完整覆盖当前 session 的控件图片引用。
- 无其他阻塞项。
