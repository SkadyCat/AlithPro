# 交付说明

- 源任务文件: `msg-2026-03-29T16-57-57-281Z.md`
- 任务目标: 给当前模板预览加上一张背景图，并把结果创建回 `uieditor` 当前模板中。

## 所做改动

- 改写了 `application\uieditor\public\app.js`：
  - `previewBoxStyles()` 现在支持 `bgImage` 与 `bgSize`，会按 `url(...) no-repeat center/{bgSize}` 渲染背景图。
- 更新了 `application\uieditor\template\bag_button.session`：
  - 给根节点 `Overlay_18` 增加了：
    - `bgImage: "/assets/bag/bg_main.png"`
    - `bgSize: "cover"`
- 新增了可直接被 `uieditor` 静态服务访问的背景图资源：
  - `application\uieditor\public\assets\bag\bg_main.png`
  - 该文件来自工作区内已有的 `canvas-editor` 资源拷贝，避免引用到 `uieditor` 无法直接访问的目录。

## 验证结果

- `node --check application\workspace\UIEditor\application\uieditor\public\app.js` 通过。
- 已确认 `bag_button.session` 根节点存在：
  - `bgImage = /assets/bag/bg_main.png`
  - `bgSize = cover`
- 已确认 `http://127.0.0.1:3210/assets/bag/bg_main.png` 返回 HTTP `200`。

## 剩余风险或阻塞项

- 当前背景图已经能被 `uieditor` 访问并用于模板预览；如果后续要继续替换成其他图片，建议也放在 `application\uieditor\public\assets\...` 下，避免跨应用静态目录不可访问的问题。
- 当前无阻塞项。
