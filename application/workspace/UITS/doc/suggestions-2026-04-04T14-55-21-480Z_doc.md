# 交付说明：suggestions-2026-04-04T14-55-21-480Z

## 源任务文件
`suggestions-2026-04-04T14-55-21-480Z.md`

## 任务目标
1. EntryClass 不作为节点树子节点，而是作为 TileView 的属性
2. left_menu 提供 EntryClass 预览菜单
3. 点击后在 center_area 单独预览 EntryClass
4. 支持设置预览数量

## 所做改动

### editor.html
- **left_menu** 新增 "EntryClass 预览" 分区（`#entryClassList`），显示所有注册的 entry class
- **EntryClass 注册系统**：`ENTRY_CLASSES` 字典 + `registerEntryClass(tag, label, sampleData)`
- **loadEntryPreview(tag)** 函数：
  - 在 center_area 渲染单个 Entry 预览（虚线框内）
  - 带预览数量输入框（1-24），点击刷新按钮重新渲染
  - 下方 TileView 使用 entry-class 渲染 N 个 entry 实例
  - 节点树显示 EntryClass 内部结构（Icon/Label/Sub/Qty）
  - 点击面板列表可返回界面编辑模式
- **Inspector TileView 属性** 新增"🧩 预览"链接行（当有 EntryWidgetClass 时）

### panels/bag-panel.js
- 节点树移除 `EntryClass: UTileEntry` 子节点，仅保留 Item01-Item12 (UTileEntry) 作为实例节点

## 验证结果
- `node --check` 全部通过
- `http://127.0.0.1:3200/editor.html` HTTP 200（41603 bytes）

## 剩余风险
- 无
