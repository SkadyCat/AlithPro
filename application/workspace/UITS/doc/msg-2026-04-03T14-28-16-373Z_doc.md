源任务文件：`msg-2026-04-03T14-28-16-373Z.md`

任务目标：
- 让按钮底图内里尽量少些花纹，整体更干净。

所做改动：
- 更新 `application\public\button-canvas.css`：
  - 去掉按钮内部两侧角饰与顶部玉石高光；
  - 收敛顶部高光与底部暗纹强度；
  - 保留一层更克制的内框与基础明暗层次。
- 更新 `application\public\button-canvas.js`：
  - 生成和栅格化的按钮结构只保留 `frame-line`，不再注入多余装饰层。

验证结果：
- `npm run check` 通过。
- `http://127.0.0.1:3200/button-canvas.html`、`button-canvas.js`、`button-canvas.css` 返回 200。
- 页面源码中仍未包含按钮内嵌文字，且脚本已不再注入 `ornament`、`jade` 等复杂装饰节点。

剩余风险或阻塞项：
- 当前已明显更简洁，但仍属于“国风游戏按钮底图”方向；如果后续需要更极简或更厚重，还需继续按具体题材微调配色和边框质感。
