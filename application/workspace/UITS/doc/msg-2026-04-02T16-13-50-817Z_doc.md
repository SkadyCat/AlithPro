源任务文件：`msg-2026-04-02T16-13-50-817Z.md`

任务目标：
- 专门用一个 canvas 区域绘制 UI。
- 新增 left_menu，用于切换不同的 UI 场景。

所做改动：
- 更新 `application\public\index.html`：
  - 将中部预览区改成三栏工作区：`left_menu`、中央 `canvas`、右侧详情说明。
  - 新增 `left-menu` 容器与 `ui-canvas` 画布。
- 更新 `application\public\styles.css`：
  - 为 `canvas-workspace`、`left-menu-card`、`left-menu-item`、`canvas-shell` 和 `ui-canvas` 增加布局样式。
  - 不同主题下仍保留布局差异，同时兼容 canvas 工作区。
- 更新 `application\public\app.js`：
  - 新增 `canvasViews` 数据，内置 3 个 UI 场景：`背包 UI`、`角色面板`、`商店 UI`。
  - 新增 `renderLeftMenu()`，通过左侧菜单切换当前场景。
  - 新增 canvas 绘制逻辑：根据当前场景在 `ui-canvas` 中绘制不同界面草图。
  - 右侧详情卡改为展示当前场景信息，并支持“刷新当前画布”。

验证结果：
- `npm run check` 通过。
- 访问 `http://127.0.0.1:3200/` 返回包含 `left-menu`、`ui-canvas`、`canvas-shell` 的页面 HTML。

剩余风险或阻塞项：
- 当前 canvas 场景是静态绘制草图，尚未接入更细的控件级交互；若要继续往可编辑设计器方向推进，需要给 canvas 图元建立可选中对象模型并与检视器联动。
