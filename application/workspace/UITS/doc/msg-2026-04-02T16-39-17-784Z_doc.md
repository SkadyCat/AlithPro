源任务文件：`msg-2026-04-02T16-39-17-784Z.md`

任务目标：
- 让 `center_area` 更像真实的游戏 UI 界面，而不是抽象概要块。

所做改动：
- 更新 `application\public\app.js`：
  - 为 canvas 绘制增加 `drawText()` 工具函数。
  - 将三种场景的画布内容从“块状概览”升级为更接近真实游戏界面的内容：
    - `主 HUD`：加入等级、HP、金币、钻石、时间、任务追踪、小地图文字与技能名称。
    - `背包界面`：加入分类按钮、容量、道具名称、品质、属性、说明文本和操作按钮。
    - `剧情对话`：加入角色名、身份、对话文本和三项可选分支按钮。
- 保留现有工作台结构不变，仅强化 `center_area` 内的真实感。

效果结论：
- 现在 `center_area` 更接近“游戏截图式预览”，而不是只用几个矩形说明布局。
- 用户可以更直观看到不同界面的真实观感、文字密度和控件形态。

验证结果：
- `npm run check` 通过。
- 访问 `http://127.0.0.1:3200/` 返回包含 `center_area`、`preview-canvas`、`left_menu`、`right_info` 的页面 HTML。

剩余风险或阻塞项：
- 当前仍然是 canvas 静态绘制，没有把这些按钮、物品格、文本区域拆成独立可编辑对象；若继续深挖，需要引入真正的 UI 对象模型。
