源任务文件：`msg-2026-04-03T14-40-28-218Z.md`

任务目标：
- 去掉按钮底图内部残留的“奇怪的线”，让内部光效只是光效，不再出现线框感。

所做改动：
- 更新 `application\public\button-canvas.css`：
  - 移除内部伪元素上的边框线；
  - 去掉内部 `frame-line` 线框结构；
  - 把内部效果改为更柔和的顶部高光与底部暗部渐变，不再形成明显线条。
- 更新 `application\public\button-canvas.js`：
  - 按钮底图 DOM 改成纯 `button-bg` 容器；
  - 页面展示与 canvas 栅格化均不再注入 `frame-line`。

验证结果：
- `npm run check` 通过。
- `http://127.0.0.1:3200/button-canvas.html`、`button-canvas.js`、`button-canvas.css` 返回 200。
- 已确认 JS 与 CSS 中不再包含 `frame-line`。

剩余风险或阻塞项：
- 当前内部已明显干净；若后续还要继续收敛，主要只剩外框金属感和整体配色可再微调。
