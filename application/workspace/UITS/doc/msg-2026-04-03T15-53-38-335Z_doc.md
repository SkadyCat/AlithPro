源任务文件：`msg-2026-04-03T15-53-38-335Z.md`

任务目标：
- 把生成图片四周的“非内容边缘”真正裁掉，而不是只清理边缘采样。
- 在交付文档里给出图片路径。

所做改动：
- 更新 `application\godot_inventory\tools\generate_bag_undead_assets.js`：
  - 在完成绘制和边缘 alpha 清理后，扫描整张图的非透明像素包围盒；
  - 按包围盒直接裁切输出 PNG；
  - 在 `manifest.json` 中额外记录：
    - `originalSize`
    - `trimmedSize`
    - `cropOffset`
- 重新执行：
  - `npm run generate:bag-undead-assets`
  - `npm run apply:bag-undead-assets`
  - `npm run generate:bag-godot-ui`

图片路径：
- 导出包目录：
  - `application\godot_inventory\exports\bag_undead_pack\assets\ui\title_plate.png`
  - `application\godot_inventory\exports\bag_undead_pack\assets\ui\topbar_bg.png`
  - `application\godot_inventory\exports\bag_undead_pack\assets\icons\tab_weapon.png`
- 当前 Godot 正在使用的活跃资源路径：
  - `application\godot_inventory\assets\ui\title_plate.png`
  - `application\godot_inventory\assets\ui\topbar_bg.png`
  - `application\godot_inventory\assets\icons\tab_weapon.png`
- 裁剪信息清单：
  - `application\godot_inventory\exports\bag_undead_pack\manifest.json`

验证结果：
- `node --check godot_inventory\tools\generate_bag_undead_assets.js` 通过。
- `npm run generate:bag-undead-assets` 通过。
- `npm run apply:bag-undead-assets` 通过。
- `npm run generate:bag-godot-ui` 通过。
- `npm run check` 通过。
- headless Godot 项目加载通过。
- 抽样确认已按内容裁剪：
  - `assets/ui/title_plate.png`：`380x84 -> 372x76`，边界检查 `MIN=0,0 MAX=371,75`
  - `assets/ui/topbar_bg.png`：`640x140 -> 624x124`，边界检查 `MIN=0,0 MAX=623,123`
  - `assets/icons/tab_weapon.png`：`64x64 -> 50x57`，边界检查 `MIN=0,0 MAX=49,56`

剩余风险或阻塞项：
- 现在导出的图片已按内容裁切，因此纹理原始尺寸发生变化；后续如果再继续大幅调整 UI 结构，需要留意 Godot 中各 TextureRect/Button 的显示比例是否仍符合预期。
