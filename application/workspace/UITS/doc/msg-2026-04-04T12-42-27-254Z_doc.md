# 交付说明 — msg-2026-04-04T12-42-27-254Z

## 源任务
`msg-2026-04-04T12-42-27-254Z.md` — 节点树右键也弹出菜单，和选中控件效果一样。

## 目标
在节点树的节点行上右键，弹出与画布控件相同的右键菜单，同时联动选中对应的 DOM 元素并显示属性面板。

## 改动

### application/elements/UTreeView.js
- 节点行新增 `contextmenu` 事件监听：阻止默认菜单，高亮当前行，派发 `OnItemContextMenu` 自定义事件（携带 label/path/clientX/clientY）。

### application/public/bag-preview-umg.html
- 监听 `bagTree` 的 `OnItemContextMenu` 事件：
  - 通过 `findDomByTreeLabel()` 定位对应 DOM 元素。
  - 自动开启检查器、高亮 DOM 元素、渲染属性面板。
  - 在鼠标位置显示右键菜单（与画布右键菜单完全一致）。

## 验证
- UTreeView.js node --check 通过。
- HTTP 200，页面 33KB 正常加载。
- 树节点右键弹出菜单，选中/属性面板/修改意见功能与画布右键一致。

## 风险
- 无。
