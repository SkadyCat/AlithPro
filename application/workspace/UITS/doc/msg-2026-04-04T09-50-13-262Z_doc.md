# 交付说明 — msg-2026-04-04T09-50-13-262Z

## 源任务
`msg-2026-04-04T09-50-13-262Z.md` — 选中控件时，节点树同步展开并高亮对应节点。

## 目标
检查器选中某控件后，右侧节点树自动展开祖先节点并高亮匹配的行，使控件与树保持联动。

## 改动
- `application/elements/UTreeView.js`
  - 新增 `.node-row.highlighted` 样式（蓝色背景 + outline）。
  - 新增 `highlightNode(text)` 方法：按 label 部分匹配搜索，展开所有祖先 `.collapsed` 节点，高亮并滚动到匹配行。
  - 新增 `clearHighlight()` 方法。

- `application/public/bag-preview-umg.html`
  - 新增 `TREE_LABEL_MAP` / `TAB_CAT_MAP`：元素 id/data-cat → 树节点 label 映射。
  - 新增 `syncTreeHighlight(el)` 函数：根据映射调用 `bagTree.highlightNode()`。
  - Inspector 点击时调用 `syncTreeHighlight(target)`，清除时调用 `bagTree.clearHighlight()`。

## 验证
- UTreeView.js node --check 通过。
- HTTP 200，页面 22KB 正常加载。
- 选中控件后节点树展开对应路径并高亮；关闭检查器后清除高亮。

## 风险
- 无。
