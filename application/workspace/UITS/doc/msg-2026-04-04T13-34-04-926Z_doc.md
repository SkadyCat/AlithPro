# 交付说明 — msg-2026-04-04T13-34-04-926Z

## 源任务
`msg-2026-04-04T13-34-04-926Z.md` — 提交到 UITS 失败 "No suggestions to submit"。

## 目标
修复"提交到 UITS"按钮在编辑器有内容但未手动保存时报错的问题。

## 根因
用户在编辑器中输入了修改意见后直接点击"提交到 UITS"，但当前逻辑要求先点"保存意见"再点"提交"。编辑器内容未保存到 suggestions 文件夹，导致提交时找不到文件。

## 改动
- `application/public/bag-preview-umg.html`
  - "提交到 UITS"按钮点击时，先检查编辑器是否有内容，若有则自动调用 `/api/save-suggestion` 保存当前意见。
  - 保存成功后再执行 `/api/submit-suggestions` 汇总提交。
  - 自动保存失败则中断并提示错误。

## 验证
- HTTP 200，页面 33KB 正常加载。
- 测试：save → submit 流程通过，文件正确生成到 sessions/。
- 直接点"提交到 UITS"时编辑器内容会自动保存后提交。

## 风险
- 无。
