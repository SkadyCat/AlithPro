源任务文件：`msg-2026-04-03T15-47-55-520Z.md`

任务目标：
- 去掉生成图片边缘里那种“非像素取样”的无效边缘，让 PNG 外轮廓更干净。

所做改动：
- 更新 `application\godot_inventory\tools\generate_bag_undead_assets.js`：
  - 新增 `cleanSampledEdges()` 后处理；
  - 专门处理“贴着透明区的半透明像素”；
  - 对这类边缘像素做 0/255 alpha 清理，去掉抗锯齿造成的无效半透明边缘；
  - 不修改内部非边缘区域的柔光层。
- 重新执行：
  - `npm run generate:bag-undead-assets`
  - `npm run apply:bag-undead-assets`
  - `npm run generate:bag-godot-ui`

验证结果：
- `node --check godot_inventory\tools\generate_bag_undead_assets.js` 通过。
- `npm run generate:bag-undead-assets` 通过。
- `npm run apply:bag-undead-assets` 通过。
- `npm run generate:bag-godot-ui` 通过。
- `npm run check` 通过。
- 对活跃资源抽样检查得到：
  - `TITLE_EDGE_SEMIALPHA=0`
  - `TOPBAR_EDGE_SEMIALPHA=0`
  - `ICON_EDGE_SEMIALPHA=0`

剩余风险或阻塞项：
- 当前清理策略只针对“贴边透明区”的半透明像素；如果后续还想进一步做更严格的像素风处理，需要额外决定是否连内部柔光也一起离散化。
