# 交付说明 — msg-2026-04-04T14-02-47-743Z

## 源任务
`msg-2026-04-04T14-02-47-743Z.md` — 点击卡片弹窗显示 Markdown 渲染内容。

## 目标
点击修改意见卡片时弹出预览窗口，将 Markdown 源码渲染为格式化 HTML 展示。

## 改动

### bag-preview-umg.html
- **新增 Markdown 预览弹窗** `.md-preview-overlay`：标题显示文件名，内容区渲染 Markdown。
- **新增 `renderMarkdown(md)` 函数**：纯前端轻量 Markdown→HTML 转换，支持 h1-h3、粗体、斜体、行内代码、代码块、引用、无序列表、水平线、段落。
- **新增 `openMdPreview(filename, content)`**：渲染并打开预览弹窗。
- **卡片点击行为更新**：点击卡片除选中外，自动打开 Markdown 渲染预览。
- **`currentCardItems` 追踪**：`renderCardsInto` 刷新时同步缓存数据供预览使用。

### CSS
- 新增 `.md-preview-overlay`、`.md-preview-box`、`.md-preview-body` 及 Markdown 元素（h1-h3, p, ul, code, pre, blockquote, strong, em）样式。

## 验证
- HTTP 200，页面 47KB 正常加载。
- 点击卡片弹出格式化预览，点击遮罩或关闭按钮可关闭。

## 风险
- Markdown 渲染为轻量正则实现，不支持表格、嵌套列表等复杂语法。
