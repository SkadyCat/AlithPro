# 交付说明：msg-2026-04-03T19-56-08-623Z

## 源任务文件
- `inprocess\msg-2026-04-03T19-56-08-623Z.md`

## 任务目标
- 回应“默认值”缺失的问题，补强当前 `bag.bp -> AIGC 任务包` 流水线的默认参数能力。
- 让 `prepare:bag-aigc` 生成结果与**当前**暗黑风背包蓝图保持一致，而不是沿用旧版资源模型。

## 所做改动
- 重写 `application\godot_inventory\tools\prepare_bag_aigc.js`
  - 改为直接读取当前 `bag.bp` 中声明的资源列表。
  - 为当前背包资源族补齐默认 profile / group / priority / prompt / references / postprocess。
  - 自动从现有 PNG 读取尺寸，避免手工维护一套过期的 size 表。
  - 在任务包与单文件 prompt 中写入统一默认值：
    - `theme`
    - `visualKeywords`
    - `colorPalette`
    - `variantBatch`
    - `backgroundMode`
    - `outputPolicy`
  - 生成前清理旧的 `aigc\prompts\*.txt`，避免残留旧版 `topbar` / `detail_panel` 等任务文件。
- 更新 `application\godot_inventory\aigc\README.md`
  - 补充默认值策略说明。
  - 改成围绕当前 `scene_bg / capacity_plate / inventory_panel_bg / item_card* / tab_* / item_*` 资源族说明工作流。

## 验证结果
- `node --check godot_inventory\tools\prepare_bag_aigc.js` 通过。
- `npm run prepare:bag-aigc` 通过，生成当前背包蓝图对应的 **20** 个任务。
- `npm run check` 通过。
- 新的 `bag-task-pack.json` 已包含：
  - `capacity_plate`
  - `close_button_bg`
  - `tab_equipment / tab_item / tab_material / tab_currency`
  - `item_coin`
- 旧版不再属于当前蓝图的 prompt 文件已被清理。

## 剩余风险或阻塞项
- 当前仍然只有 **AIGC 任务准备层**，还没有直接接入实际模型出图 API；这次修复的是默认值和任务包质量，不是新增第三方调用端。
