源任务文件：`msg-2026-04-02T16-10-32-639Z.md`

任务目标：
- 在 3200 演示页顶部新增一个 top menu。
- 将检视器功能改成顶部菜单中的 checkbox 入口。
- 支持按 `Esc` 关闭检视器。

所做改动：
- 更新 `application\public\index.html`：
  - 在页面顶部新增 `top-menu` 控制条。
  - 加入 `inspector-checkbox`，把检视器入口从右下角悬浮按钮改为顶部 checkbox。
- 更新 `application\public\styles.css`：
  - 为顶部菜单、品牌信息和 checkbox 开关增加样式。
  - 删除旧的右下角检视器浮动按钮样式。
- 更新 `application\public\app.js`：
  - 将检视器开关控制改为 `inspector-checkbox`。
  - 新增 `Escape` 键关闭逻辑。
  - 保持原有 `F12` 快捷开关行为不变，并与 checkbox 状态同步。

验证结果：
- `npm run check` 通过。
- 访问 `http://127.0.0.1:3200/` 返回包含 `top-menu`、`inspector-checkbox`、`F12 检视器` 的页面 HTML。

剩余风险或阻塞项：
- 无。
