# 交付说明 — 补完布局 / ListView / TileView 控件

## 源任务
`msg-2026-04-04T09-27-05-265Z.md` — "布局控件，tileview/listview控件都没有诶？"

## 目标
增强 elements 控件库中 ListView / TileView 的实现（选中态、空态、声明式 items 属性），并创建可视化演示页面验证全部 34 个控件可用。

## 改动

### 修改
- **elements/UDataViews.js**
  - `UListView`: 新增选中态高亮、`selection-mode` 属性（Single/Multi）、声明式 `items` JSON 属性、空数据提示、自动标签/副标题渲染
  - `UTileView`: 新增选中态、图标/标签/副标题自动渲染、声明式 `items` 属性、`gap` 属性、卡片式样式

### 新增
- **public/elements-demo.html** — 全控件可视化演示页面，涵盖：
  - 输入控件: Button×3、CheckBox×2、Slider、SpinBox、ComboBox、EditableText(Box)、MultiLine
  - 文字: TextBlock×3（普通/大号/阴影）、RichTextBlock
  - 视觉: ProgressBar×3、Throbber×2、BackgroundBlur
  - 容器: HBox、VBox、WrapBox、UniformGridPanel、Overlay、WidgetSwitcher（3 页切换）、ScrollBox、SizeBox、Spacer、Border
  - 数据驱动: ListView（8 条目带选中）、TileView（8 图标网格）、TreeView（背包节点树）、MenuAnchor（弹出菜单）

## 验证
- UDataViews.js `node --check` 通过
- `http://127.0.0.1:3200/elements-demo.html` 返回 200，13465 bytes

## 风险
- 无
