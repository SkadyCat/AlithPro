源任务文件：`msg-2026-04-03T13-33-48-089Z.md`

任务目标：
- 生成与当前 `bag.bp` / AIGC 方案对应的正式资产。

所做改动：
- 更新 `application\package.json`，新增 `npm run build:bag-ui`。
- 新增 `application\godot_inventory\tools\build_bag_ui.js`：
  - 先执行 `prepare_bag_aigc.js`，刷新 AIGC 任务包与 prompts；
  - 再执行 `generate_bag_assets.js`，生成正式的背包 UI / icon 资源；
  - 最后输出 `application\godot_inventory\aigc\generated-assets.json`，记录每个资产的路径、文件大小、修改时间和关联 prompt。

落地结果：
- 已生成并确认 `bag.bp` 中声明的 34 个资产。
- 当前仓库已有一条完整的一键链路：
  - `npm run prepare:bag-aigc`：准备 AIGC 任务包；
  - `npm run generate:bag-assets`：用现有确定性脚本生成正式资源；
  - `npm run build:bag-ui`：串联两者并产出最终资产清单，但**不会直接调用 AIGC 模型服务**。
- `generated-assets.json` 现在可以作为本轮产物索引，方便后续回归和人工筛选替换。

验证结果：
- `node --check godot_inventory\tools\build_bag_ui.js` 通过。
- `npm run build:bag-ui` 通过，输出 34 个资产的生成清单。
- `npm run check` 通过。

剩余风险或阻塞项：
- 当前正式资产仍来自现有确定性脚本生成器；如果下一步要替换成更高质感的外部 AIGC 结果，可沿用 `bag-task-pack.json` 和 `prompts\*.txt` 对应覆盖，再继续复用 `generated-assets.json` 追踪产物。
