源任务文件：`msg-2026-04-03T15-33-26-274Z.md`

任务目标：
- 确认 Godot 当前使用的是不是最新 UI。
- 如果不是，切换到最新 UI 并重新生成场景。

所做改动：
- 核对发现此前 `bag_ui.tscn` 虽然重生成了，但仍引用 `res://assets/...` 下的旧活跃资源，而不是最新导出的 `exports\bag_undead_pack`。
- 新增 `application\godot_inventory\tools\apply_bag_undead_assets.js`：
  - 把 `application\godot_inventory\exports\bag_undead_pack\assets\ui` 与 `assets\icons` 同步覆盖到当前活跃 `application\godot_inventory\assets\`。
- 更新 `application\package.json`：
  - 新增命令 `npm run apply:bag-undead-assets`。
- 执行：
  - `npm run apply:bag-undead-assets`
  - `npm run generate:bag-godot-ui`
  - 让 `bag_ui.tscn` 基于最新活跃资源重新生成。

验证结果：
- `node --check godot_inventory\tools\apply_bag_undead_assets.js` 通过。
- `npm run apply:bag-undead-assets` 通过。
- `npm run generate:bag-godot-ui` 通过。
- `npm run check` 通过。
- 关键资源哈希已确认与最新导出包一致：
  - `assets\ui\title_plate.png`
  - `assets\ui\topbar_bg.png`
  - `assets\ui\detail_title_plate.png`
  - `assets\icons\tab_weapon.png`
- `application\godot_inventory\scenes\bag_ui.tscn` 存在。
- headless Godot 项目加载通过。

剩余风险或阻塞项：
- 当前已切到最新亡灵风活跃资源；如果后续继续修改 `exports\bag_undead_pack`，还需要再次执行 `npm run apply:bag-undead-assets` 才会同步到 Godot 当前使用的资源目录。
