# 交付说明：msg-2026-04-03T19-30-58-450Z

## 源任务文件
- `inprocess\msg-2026-04-03T19-30-58-450Z.md`

## 任务目标
- 基于用户提供的 `layout-annotation.v1` JSON，重做一版新的背包：
  - 更新 `bag.bp`
  - 生成对应素材
  - 生成新的 Godot UI 场景

## 所做改动
- 更新 `application\godot_inventory\bag.bp`
  - 背包结构改为：
    - 容量信息区 `CapacityPlate / CapacityLabel`
    - 关闭按钮 `CloseButton`
    - 左侧分类页签 `TabEquipment / TabItem / TabMaterial / TabCurrency`
    - 中部动态格子区 `InventoryPanel / GridContainer`
- 更新 `application\godot_inventory\layout\bag_dark_layout.json`
  - 按任务中的 JSON 坐标重写布局
  - 区域映射为：
    - `grid`
    - `tabs`
    - `close`
    - `capacity`
- 更新 `application\godot_inventory\tools\generate_bag_ui.gd`
  - 场景生成逻辑改为匹配新的节点树和区域种类
  - 调整页签、格子尺寸和间距
- 更新 `application\godot_inventory\scripts\main.gd`
  - 分类改为 `equipment / item / material / currency`
  - 页签图标与数据源同步切换
  - 容量文本改为顶部 `08 / 12`
  - 格子数据改为新布局对应的 8 个物品条目
- 更新 `application\godot_inventory\tools\generate_bag_undead_assets.js`
  - 新增 `capacity_plate.png`
  - 新增 `tab_equipment.png / tab_item.png / tab_material.png / tab_currency.png`
  - 新增 `item_coin.png`
  - 同步调整面板、页签、格子、关闭按钮资源尺寸

## 验证结果
- 已执行：
  - `npm run generate:bag-undead-assets`
  - `npm run apply:bag-undead-assets`
  - `npm run generate:bag-godot-ui`
  - `Godot --headless --path ... --quit`
- 中途补做了一次 Godot 资源导入，以生成 `capacity_plate.png.import`
- 最终 `res://scenes/bag_ui.tscn` 已按新布局成功生成

## 剩余风险或阻塞项
- 当前 `exports\bag_undead_pack\assets\...` 与 `assets\...` 会同时存在，Godot 导入时会出现一批 UID duplicate warning，但不影响本次背包主链生成结果。
