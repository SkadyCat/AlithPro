源任务文件：`msg-2026-04-03T14-19-08-063Z.md`

任务目标：
- 去掉按钮底图中的文本。
- 把视觉从偏欧美按钮改成更接近游戏 UI 的风格。

所做改动：
- 更新 `application\public\button-canvas.html`：
  - 左侧示例改成纯底图，不再在按钮内写任何文字；
  - 页面说明同步强调“底图不带字，后续可单独叠字”。
- 更新 `application\public\button-canvas.css`：
  - 去掉原来的英文铭文和主标题文字样式；
  - 把按钮底图改成更偏游戏 UI / 国风器物感的铜金边框、深红漆底、玉石高光和中式装饰结构。
- 更新 `application\public\button-canvas.js`：
  - Canvas 栅格化内容改为纯按钮底图；
  - 运行时只注入装饰层，不再注入任何文字。

可打开地址：
- `http://127.0.0.1:3200/button-canvas.html`

验证结果：
- 页面、JS、CSS 地址均返回 200。
- 页面源码与样式中已确认不再包含 `开始锻造` 或 `ARCANE FORGE` 等内嵌按钮文字。

剩余风险或阻塞项：
- 当前只是单个按钮底图方向探索；如果下一步要继续贴近特定游戏题材，还需要进一步细化题材关键词，例如仙侠、暗黑、二次元或科幻。
