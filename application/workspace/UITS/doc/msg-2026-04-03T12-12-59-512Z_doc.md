源任务文件：`msg-2026-04-03T12-12-59-512Z.md`

任务目标：
- 创建一个 `bag.bp`。
- 内容包含背包的基本结构、树状层级、使用的控件以及所需资源。

所做改动：
- 在 `application\godot_inventory\bag.bp` 新增蓝图说明文件，放入现有 Godot 背包原型目录，便于与场景、脚本、资源目录一起维护。
- 结合当前仓库已有的 Web 背包预览与 Godot 背包原型，补全了：
  - 顶层分区与节点树；
  - 每个区域建议使用的控件类型；
  - 现有可复用的 UI 贴图与图标资源清单；
  - 最小数据字段与交互状态说明。

验证结果：
- 已确认仓库中不存在既有 `.bp` 样例，因此本次以可读性优先的蓝图说明格式交付，并放在最贴近背包实现的 `application\godot_inventory\` 目录。
- 已对照 `application\public\app.js` 与 `application\godot_inventory\scripts\main.gd`，确保 `bag.bp` 中的背包分区、节点命名和资源命名与现有原型一致。

剩余风险或阻塞项：
- `bag.bp` 当前是设计蓝图说明文件，不是某个现成导入器的机器可执行格式；如果后续需要被工具直接消费，需要再约定语法或转换脚本。
