# 交付说明：msg-2026-04-03T19-14-30-507Z

## 源任务文件
- `inprocess\msg-2026-04-03T19-14-30-507Z.md`

## 任务目标
- 更换当前 Markdown 编辑器插件，解决按空格后游标异常跳回首行的问题。

## 所做改动
- 更新 `application\public\layout-annotator.html`
  - 移除 Ace CDN 依赖
  - 接入 CodeMirror 5 的样式与脚本
  - 编辑器说明改为 CodeMirror
- 更新 `application\public\layout-annotator.css`
  - 新增 CodeMirror 深色外观样式
  - 保持与当前 layout-annotator 面板视觉一致
- 更新 `application\public\layout-annotator.js`
  - 将描述区编辑器从 Ace 切换为 CodeMirror
  - 保留现有 session 保存、Markdown 预览、工具栏包裹逻辑
  - 修正 `bindField()`：描述字段不再使用 `.trim()`，保留真实空格和换行输入

## 验证结果
- 已执行 `npm run check`
- 已确认页面代码中：
  - 通过 `window.CodeMirror.fromTextArea(...)` 初始化编辑器
  - 已移除 Ace 依赖
  - 描述字段改为 `key === "description" ? input.value : input.value.trim()`

## 剩余风险或阻塞项
- CodeMirror 目前仍通过 CDN 加载；如果后续需要完全离线可用，可再转为本地静态资源。
