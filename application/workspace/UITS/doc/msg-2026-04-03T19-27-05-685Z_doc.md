# 交付说明：msg-2026-04-03T19-27-05-685Z

## 源任务文件
- `inprocess\msg-2026-04-03T19-27-05-685Z.md`

## 任务目标
- 为 layout-annotator 中已绘制的框增加右键删除能力。

## 所做改动
- 更新 `application\public\layout-annotator.js`
  - 新增 `removeBoxById(boxId, statusText)` 共用删除逻辑
  - 每个画布框体新增 `contextmenu` 事件，右键时直接删除对应框
  - 拖拽入口增加 `event.button !== 0` 判断，避免右键触发拖拽逻辑
  - 顶部“删除选中框”按钮改为复用同一套删除逻辑
  - 删除后会沿用现有自动保存链路写回当前 session

## 验证结果
- 已执行 `npm run check`
- 已确认代码中存在：
  - `removeBoxById(...)`
  - `node.addEventListener("contextmenu", ...)`
  - `event.button !== 0`

## 剩余风险或阻塞项
- 无。
