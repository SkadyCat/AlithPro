源任务文件：`msg-2026-04-03T15-21-38-126Z.md`

任务目标：
- 修正亡灵风素材包里的 `title_plate`，去掉多余、奇怪的装饰，让生成结果更纯粹。

所做改动：
- 更新 `application\godot_inventory\tools\generate_bag_undead_assets.js`：
  - 调整 `drawPlate(kindName)` 逻辑；
  - 现在只有 `currency_plate` 保留右侧印记；
  - `title_plate`、`detail_title_plate`、`grid_header_plate` 不再附带额外印记装饰。
- 重新生成 `application\godot_inventory\exports\bag_undead_pack\` 整套资源。

验证结果：
- `node --check godot_inventory\tools\generate_bag_undead_assets.js` 通过。
- `npm run generate:bag-undead-assets` 通过。
- `npm run check` 通过。
- 已确认重新导出了：
  - `assets\ui\title_plate.png`
  - `assets\ui\detail_title_plate.png`
  - `assets\ui\currency_plate.png`
  - `manifest.json` 中仍为 `assetCount=34`

剩余风险或阻塞项：
- 当前已去掉牌匾上的额外印记；如果后续还想继续“更纯”，可以再进一步收敛局部光感或边框亮度。
