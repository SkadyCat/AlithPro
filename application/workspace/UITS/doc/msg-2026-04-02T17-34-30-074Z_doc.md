# 交付说明

- 源任务文件：`msg-2026-04-02T17-34-30-074Z.md`
- 任务目标：不要再把 Godot 的 `TabWeapon` 当纯文本按钮，需要为它生成一个真实背景图，并塞回 Godot 场景里。

## 所做改动

- 新增 `application\godot_inventory\tools\generate_tab_weapon_texture.js`。
  - 使用 Node.js + Playwright 启动无头 Chromium。
  - 在 canvas 上绘制按钮底图，再导出为 PNG。
- 生成并写入实际资源：
  - `application\godot_inventory\assets\tab_weapon_bg.png`
- 更新 `application\godot_inventory\scenes\main.tscn`：
  - 为 `TabWeapon` 增加 `Texture2D` 外部资源引用。
  - 新增 `StyleBoxTexture` 子资源。
  - 把 `normal / hover / pressed / focus` 样式挂到 `TabWeapon` 上。
  - 调整字体颜色与字号，让按钮文字和新背景图匹配。

## 验证结果

- 已确认 PNG 资源真实生成：
  - `application\godot_inventory\assets\tab_weapon_bg.png`
- 已确认 Godot 重新加载场景时成功读取：
  - `res://assets/tab_weapon_bg.png`
  - `res://.godot/imported/tab_weapon_bg.png-91f578a32538d9c7b05f1ad09d9f083d.ctex`
- 说明 `TabWeapon` 现在不再只是纯按钮底色，而是接入了真实背景图资源。

## 剩余风险或阻塞项

- 当前只为 `TabWeapon` 补了背景图；如果后续要求 `TabAll`、`TabPotion`、`TabQuest` 也做成成套贴图，还需要继续补对应资产。
