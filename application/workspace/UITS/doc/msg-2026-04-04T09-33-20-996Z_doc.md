# 交付说明 — 拆分为每控件独立文件

## 源任务
`msg-2026-04-04T09-33-20-996Z.md` — "我没看到你创建 Horizontal之类的 控件啊，tileview也没有...我要的是完整的"

## 目标
将合并的控件文件拆分为每个控件一个独立 `.js` 文件，让用户在 `elements/` 目录中可以直接看到 `UHorizontalBox.js`、`UTileView.js` 等所有控件。

## 改动

### 拆分 5 个合并文件 → 29 个独立文件
- `UContainers.js` → UCanvasPanel.js / UHorizontalBox.js / UVerticalBox.js / UWrapBox.js / UGridPanel.js / UUniformGridPanel.js / UOverlay.js / UWidgetSwitcher.js / UScrollBox.js / USizeBox.js / UScaleBox.js / USpacer.js / UBorder.js / USafeZone.js（14 个）
- `UInputControls.js` → UCheckBox.js / UEditableText.js / UEditableTextBox.js / UMultiLineEditableText.js / UMultiLineEditableTextBox.js / USpinBox.js / USlider.js / UComboBoxString.js（8 个）
- `UImage.js` → UImage.js / UProgressBar.js / UThrobber.js / UCircularThrobber.js / UBackgroundBlur.js（5 个）
- `UDataViews.js` → UListView.js / UTileView.js / UTreeView.js / UMenuAnchor.js（4 个）
- `UTextBlock.js` → UTextBlock.js / URichTextBlock.js（2 个）

### 删除旧合并文件
- UContainers.js、UInputControls.js、UDataViews.js 已删除

### 更新
- `index.js` — 改为从 33 个独立文件逐一导入
- `README.md` — 更新文件结构说明

## 最终目录结构
```
elements/ (36 个 JS 文件)
├── index.js          ← 入口
├── UWidget.js        ← 基类
├── UButton.js        ← 1 个按钮
├── UCheckBox.js ~ UComboBoxString.js  ← 8 个输入
├── UTextBlock.js / URichTextBlock.js  ← 2 个文字
├── UImage.js ~ UBackgroundBlur.js     ← 5 个视觉
├── UCanvasPanel.js ~ USafeZone.js     ← 14 个容器
└── UListView.js ~ UMenuAnchor.js      ← 4 个数据
```

## 验证
- 全部 36 个 JS 文件 `node --check` 通过
- 旧合并文件已删除
- `elements-demo.html` 通过 `index.js` 正常加载

## 风险
- 无
