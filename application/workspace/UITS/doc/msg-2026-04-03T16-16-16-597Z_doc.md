源任务文件：`msg-2026-04-03T16-16-16-597Z.md`

任务目标：
- 清理 `application\godot_inventory\assets\ui\topbar_bg.png` 里的“脏东西”，让图片保持干净。

所做改动：
- 定位到问题来自 `application\godot_inventory\tools\generate_bag_undead_assets.js` 中 `topbar_bg` 的生成逻辑：
  - 之前顶栏背景沿用了带标记/装饰的面板生成思路，导致背景条里出现不该有的印记。
- 更新生成逻辑：
  - `drawPanel("topbar")` 不再走带标记的面板装饰分支；
  - 新增 `drawTopbarPanel()`，只保留更干净的雾光和一层浅内带，不再绘制两侧印记或额外块。
- 重新执行：
  - `npm run generate:bag-undead-assets`
  - `npm run apply:bag-undead-assets`
  - `npm run generate:bag-godot-ui`

图片路径：
- 当前活跃资源：
  - `application\godot_inventory\assets\ui\topbar_bg.png`
- 导出包副本：
  - `application\godot_inventory\exports\bag_undead_pack\assets\ui\topbar_bg.png`

验证结果：
- `node --check godot_inventory\tools\generate_bag_undead_assets.js` 通过。
- `npm run generate:bag-undead-assets` 通过。
- `npm run apply:bag-undead-assets` 通过。
- `npm run generate:bag-godot-ui` 通过。
- `npm run check` 通过。
- `topbar_bg.png` 当前尺寸保持为 `1368x84`。
- 已重新查看 `application\godot_inventory\assets\ui\topbar_bg.png`，两侧脏印记已消失。
- headless Godot 项目加载通过。

剩余风险或阻塞项：
- 当前仅清理了顶栏背景图本身；如果后续还觉得某些区域“不干净”，需要继续区分是背景图问题，还是 `title_plate` / `currency_plate` 等独立叠加资源的问题。
