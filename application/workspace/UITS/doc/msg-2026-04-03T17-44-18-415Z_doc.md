# 交付说明：msg-2026-04-03T17-44-18-415Z

## 源任务文件
- `inprocess\msg-2026-04-03T17-44-18-415Z.md`

## 任务目标
- 说明并修复“有些控件没有设计对应图片资源”的问题。

## 所做改动
- 审核了 `application\godot_inventory\bag.bp`、`generate_bag_ui.gd`、`main.gd` 和导出清单，确认当前真正漏配的可见交互控件是 `CloseButton`。
- 更新 `application\godot_inventory\bag.bp`，把 `res://assets/ui/close_button_bg.png` 纳入正式资源声明。
- 更新 `application\godot_inventory\tools\generate_bag_undead_assets.js`，新增 `close_button_bg.png` 规格与绘制逻辑，尺寸为 `52x44`。
- 更新 `application\godot_inventory\scripts\main.gd`，让关闭按钮改用 `close_button_bg.png` 作为正式底图，而不再使用临时 `StyleBoxFlat` 程序描边。
- 更新 `application\godot_inventory\tools\generate_bag_ui.gd` 的资源路径表，使场景生成链显式认识该按钮资源。

## 结论说明
- 这次问题里，**确实应该有图片却没配上的控件** 是：
  - `CloseButton`
- 以下节点 **本来就不应单独设计图片资源**：
  - `Tabs`、`GridContainer`：它们是布局容器，不是视觉面，真正可见的是内部按钮
  - `Title`、`Currency`：它们是文字层，应该叠加在底板之上，而不是把文字烤进图片
  - `Background: ColorRect`：只是兜底纯色层，真正背景图资源已经由 `scene_bg.png` 承担

## 验证结果
- 已重新执行：
  - `npm run generate:bag-undead-assets`
  - `npm run apply:bag-undead-assets`
  - `npm run generate:bag-godot-ui`
  - `npm run check`
- 最新导出清单已变为 21 个资源，包含新增的 `close_button_bg.png`。

## 关键图片路径
- 导出包：
  - `application\godot_inventory\exports\bag_undead_pack\assets\ui\close_button_bg.png`
- 当前活跃资源：
  - `application\godot_inventory\assets\ui\close_button_bg.png`

## 剩余风险或阻塞项
- 当前容器类节点仍然不会被单独出图，这是有意保持的结构边界；如果后续要让容器本身出现额外装饰，应新增对应 `TextureRect` 视觉节点，而不是直接给容器节点塞贴图。
