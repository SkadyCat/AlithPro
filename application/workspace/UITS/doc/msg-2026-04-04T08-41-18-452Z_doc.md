# 交付说明：重新设计 bag.bp

## 源任务文件
`msg-2026-04-04T08-41-18-452Z.md`

## 目标
根据当前四区基本结构，重新设计 `application\godot_inventory\bag.bp`。

## 改动内容

### bag.bp 重写
- **Goal**：改为精确描述——暗黑风四区布局，底图不含文字。
- **[Layout]（新增）**：显式列出 bag_dark_layout.json 的 4 个区域坐标及对应节点。
- **[Controls]**：从散文式描述改为 `节点 → 类型 → 说明` 的结构化表格。
- **[Resources]**：按功能分 5 组（背景、面板、页签、格子、图标），每组带子标题。
- **[Data]**：改为带字段类型的迷你数据模式，区分物品结构和交互状态。
- **[Notes]**：精炼为 6 条关键约束，补充生成链和 AIGC 链引用。
- **[Tree]**：保持不变，确保管线兼容。

### 同步更新
- `exports\bag_undead_pack\bag.bp` 已同步覆盖。

## 验证结果
- `npm run check`：通过。
- 资源解析器：20/20 条 `- res://assets/` 行正确提取。
- 节点树解析器：26/26 节点正确提取，[Layout] 章节不干扰 [Tree] 解析。

## 风险与阻塞
- 无。结构兼容，下游 generate_bag_ui.gd、generate_bag_undead_assets.js、prepare_bag_aigc.js 不需要改动。
