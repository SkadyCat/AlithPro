# 交付说明

- 源任务文件：`msg-2026-04-02T17-38-43-618Z.md`
- 任务目标：回到 Godot 背包界面，对整体 UI 做一次完整重构；凡是需要图片的地方都补图片，并统一改成东方幻想风格。

## 所做改动

- 新增 `application\godot_inventory\tools\generate_oriental_inventory_assets.js`。
  - 通过 Node.js + Playwright + canvas 批量生成整套东方幻想 PNG 资源。
- 生成了两大类资源：
  - `application\godot_inventory\assets\ui\`
    - `scene_bg.png`
    - `topbar_bg.png`
    - `inventory_panel_bg.png`
    - `detail_panel_bg.png`
    - `tab_idle.png`
    - `tab_active.png`
    - `item_card_bg.png`
    - `item_card_selected.png`
    - `action_primary.png`
    - `action_secondary.png`
    - `action_danger.png`
  - `application\godot_inventory\assets\icons\`
    - `tab_all.png`
    - `tab_weapon.png`
    - `tab_potion.png`
    - `tab_quest.png`
    - `item_sword.png`
    - `item_potion.png`
    - `item_amulet.png`
    - `item_scroll.png`
    - `item_shield.png`
    - `item_feather.png`
    - `item_key.png`
    - `item_bow.png`
- 重写 `application\godot_inventory\scripts\main.gd`：
  - 给分页按钮接入图标和东方幻想分页底图。
  - 给物品卡接入图标、选中态卡片底图。
  - 给整理 / 拆分 / 丢弃 / 装备 / 对比按钮接入图片式按钮底图。
  - 重构背包数据和分类切换逻辑，使内容文案也统一为东方幻想语境。
  - 自动创建场景背景图层，把 `scene_bg.png` 塞到根界面背景中。
- 保留并复用上一个任务生成的 `tab_weapon_bg.png`，让 `TabWeapon` 的单独贴图仍可继续使用与迭代。

## 验证结果

- 已成功生成整套东方幻想风格图片资源。
- 已确认 Godot headless 加载场景时成功读取：
  - `res://assets/ui/*.png`
  - `res://assets/icons/*.png`
  - `res://assets/tab_weapon_bg.png`
- 已确认重写后的 `main.gd` 可以通过 Godot 脚本解析并完成场景加载，没有新的脚本错误。

## 剩余风险或阻塞项

- 当前图片风格已经统一成东方幻想，但仍属于程序化绘制的 UI 贴图，不是人工精修的商业美术资源。
- 如果后续还要进一步提升“更像正式游戏成品”的质感，可以继续补角色立绘、器物插画和更复杂的纹理细节。
