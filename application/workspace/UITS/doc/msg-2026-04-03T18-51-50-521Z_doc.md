# 交付说明：msg-2026-04-03T18-51-50-521Z

## 源任务文件
- `inprocess\msg-2026-04-03T18-51-50-521Z.md`

## 任务目标
- 保持默认画布为 1440p，并解决当前画布在页面里显示过小、难以绘制的问题。

## 所做改动
- 更新 `application\public\layout-annotator.html`
  - 新增画布缩放选择器 `canvas-zoom`
- 更新 `application\public\layout-annotator.css`
  - 将页面最大宽度扩大到 `1800px`
  - 将 `canvas-shell` 改为可滚动容器
  - 将 `design-canvas` 改为真实像素尺寸显示，而不是继续强制 `width: 100%`
- 更新 `application\public\layout-annotator.js`
  - 新增 `zoom` 状态并持久化到 session
  - 画布默认按 `100%` 显示 `1440 x 900`
  - 支持 `50% / 75% / 100% / 125%` 缩放
  - 当前画布装不下时，可直接滚动查看，不再被自动压缩成小缩略图

## 验证结果
- 已执行 `npm run check`
- 已通过页面标记检查，确认页面中存在：
  - `canvas-zoom`
  - `100%`
  - `50%`

## 剩余风险或阻塞项
- 当前是滚动查看大画布；如果后续还需要类似专业设计器的中键平移、缩放手势或小地图导航，可继续在此基础上补交互。
