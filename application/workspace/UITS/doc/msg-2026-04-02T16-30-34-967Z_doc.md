源任务文件：`msg-2026-04-02T16-30-34-967Z.md`

任务目标：
- 放弃之前的背包/风格实验界面。
- 重构为一个通用的游戏 UI 预览工作台：
  - `left_menu`：可预览的 UI 界面列表
  - `center_area`：当前预览界面
  - `top_menu`：控制功能
  - `right_info`：当前 UI 的层级信息

所做改动：
- 完整重写 `application\public\index.html`：
  - 页面结构改为 `top_menu + layout_grid`。
  - `layout_grid` 内明确分成 `left_menu`、`center_area`、`right_info` 三个区域。
  - 保留检视器侧栏，但不再让主页面承担风格实验用途。
- 完整重写 `application\public\styles.css`：
  - 移除上一版围绕背包/风格提案的大量样式。
  - 收敛为工作台样式系统，突出四大区域的职责与布局。
  - 保留主题切换、检视器侧栏和响应式布局。
- 完整重写 `application\public\app.js`：
  - 新增 `screens` 数据模型，内置 `主 HUD`、`背包界面`、`剧情对话` 三个可预览 UI。
  - `left_menu` 负责切换当前预览场景。
  - `center_area` 使用 canvas 绘制当前 UI 草图。
  - `right_info` 展示当前 UI 的层级结构说明。
  - `top_menu` 提供主题切换、刷新和层级检视开关。
  - 保留 F12 / Esc 的检视器逻辑，用于高亮工作台节点。

验证结果：
- `npm run check` 通过。
- 访问 `http://127.0.0.1:3200/` 返回包含 `top_menu`、`left_menu`、`center_area`、`right_info`、`preview-canvas` 的页面 HTML。

剩余风险或阻塞项：
- 当前 `center_area` 仍是静态 canvas 草图，不是可编辑设计器；如果下一步需要拖拽或控件级编辑，需要继续为 canvas 图元建立独立的数据模型与选中系统。
