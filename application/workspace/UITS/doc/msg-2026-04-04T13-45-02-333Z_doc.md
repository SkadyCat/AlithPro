# 交付说明 — msg-2026-04-04T13-45-02-333Z

## 源任务
`msg-2026-04-04T13-45-02-333Z.md` — 意见卡片放在页面的一个区域（内嵌而非弹窗）。

## 目标
将修改意见卡片列表直接嵌入 bag-preview-umg.html 右侧边栏，作为常驻面板，同时保留弹窗查看的选项。

## 改动

### bag-preview-umg.html
- **新增侧边栏区域** `.suggestions-inline-panel`：位于属性面板下方，包含标题（带计数徽章）、内嵌卡片列表、"📤 提交到 UITS"按钮。
- **重构卡片渲染逻辑**：抽取 `renderCardsInto(container, items)` 和 `fetchSuggestionItems()` 共享函数，同时为内嵌面板和弹窗复用。
- **新增 `refreshInlineCards()`**：保存/删除/提交后自动刷新内嵌卡片列表。
- **内嵌提交按钮**：直接从侧边栏提交所有意见到 UITS sessions。
- **右键编辑/删除**：内嵌卡片与弹窗卡片共享同一右键菜单。

### CSS
- 新增 `.suggestions-inline-panel`、`.suggestions-inline-header`、`.suggestions-inline-body` 样式。

## 验证
- HTTP 200，页面 42KB 正常加载。
- 侧边栏底部显示意见卡片列表，右键可编辑/删除，提交按钮可用。

## 风险
- 无。
