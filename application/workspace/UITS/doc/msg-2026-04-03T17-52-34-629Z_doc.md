# 交付说明：msg-2026-04-03T17-52-34-629Z

## 源任务文件
- `inprocess\msg-2026-04-03T17-52-34-629Z.md`

## 任务目标
- 根据最新补充的布局描述，重新制作一个更贴合需求的背包：
  - 格子区域：每个格子需要有背景图、icon、堆叠数量
  - 页签区域：按钮需要有背景图和文本

## 所做改动
- 更新 `application\godot_inventory\scripts\main.gd`：
  - 为物品数据补充 `quantity`
  - 背包格子不再显示物品标题和副标题
  - 改为格子内显示 `icon + x数量`
  - 物品名称、品级与描述改放到 tooltip 中
- 调整格子按钮样式，使数量文本靠右，图标尺寸更突出，更符合“格子内容是 icon”的需求。
- 更新 `application\godot_inventory\tools\generate_bag_ui.gd` 的默认格子文本，使生成出的 `bag_ui.tscn` 初始内容也符合 `x数量` 呈现。
- 保持页签按钮为“背景图 + 文本”的结构，并继续沿用已有页签底图资源。

## 验证结果
- 已执行：
  - `npm run generate:bag-godot-ui`
  - `npm run check`
- 已执行 headless Godot 项目启动确认，当前背包场景可正常加载。

## 剩余风险或阻塞项
- 当前格子里的数量仍然使用按钮文字叠加，而不是单独的数字徽标节点；如果后续要更强的 MMO/ARPG 风格角标效果，建议把每个格子扩展为独立控件树（背景、图标、角标文本分层）。
