源任务文件：`msg-2026-04-02T16-34-51-706Z.md`

任务目标：
- 让界面更接近真实分辨率预览效果。
- `left_menu` 要贴屏幕左边，而不是继续保持居中网页感。
- 修正整体风格，去掉奇怪的“卡片网页味”。

所做改动：
- 更新 `application\public\styles.css`：
  - 去掉 `workbench` 的固定最大宽度和居中容器模式。
  - 将 `top_menu` 改为固定在屏幕顶部的贴边工具条。
  - 将三栏区改为贴屏布局：`left_menu` 靠左、`center_area` 居中铺开、`right_info` 靠右。
  - 去掉主工作区的大圆角卡片感，改为更像编辑器/工具面的分栏结构。
  - 让 `canvas_panel` 高度直接跟随当前视口，接近真实工作区高度。
- 更新 `application\public\app.js`：
  - 不再固定 canvas 设计尺寸。
  - 根据 `center_area` 当前可用尺寸动态设置 `preview-canvas` 的宽高。
  - 在窗口 resize 时自动重绘，使预览更接近真实分辨率效果。
  - 各个 UI 草图的绘制参数改为基于当前 canvas 宽高进行计算。

效果结论：
- 现在页面更像一个真正的游戏 UI 工具工作台，而不是居中的营销页/展示页。
- `left_menu` 已经贴在屏幕左侧，`center_area` 会随着窗口变化按真实可用尺寸重绘。

验证结果：
- `npm run check` 通过。
- 访问 `http://127.0.0.1:3200/` 返回包含 `top_menu`、`left_menu`、`center_area`、`right_info`、`preview-canvas` 的页面 HTML。

剩余风险或阻塞项：
- 当前“真实分辨率”是基于浏览器窗口内可用区域做 canvas 自适应，并非锁定到某个设备标准分辨率；如果下一步需要严格预览 1920x1080 / 1280x720 等目标分辨率，还需要再加入分辨率档位切换。
