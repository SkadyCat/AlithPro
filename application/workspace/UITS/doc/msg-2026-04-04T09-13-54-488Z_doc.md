# 交付说明 — 右侧节点树面板

## 源任务
`msg-2026-04-04T09-13-54-488Z.md` — "在右侧，显示这个界面的节点树"

## 目标
在 bag-preview.html 的右侧添加节点树面板，展示 bag.bp 的 [Tree] 层级结构。

## 改动
- **application/public/bag-preview.html**
  - body 布局改为 `flex` 左右分栏（画布左 + 树面板右）
  - 新增 `.tree-panel` 及相关 CSS 样式（缩进、分支线、类型图标色系）
  - 新增 `TREE_NODES` 数据（26 个节点，depth 0-3，对应 bag.bp 完整树）
  - 新增 `renderTree()` 函数：生成缩进 + 分支符号 + 类型图标 + 名称/类型
  - 新增 `highlightNode()` 函数：hover 树节点时高亮对应画布 DOM 元素（outline 蓝框）
  - 节点类型色系：Control 绿、ColorRect 橙、TextureRect 蓝紫、Label 青、Button 粉、VBox 蓝、Grid 紫

## 验证
- HTTP 200，16462 bytes，包含 `tree-panel` 标记
- 节点树与 bag.bp [Tree] 完全对应
- hover 联动高亮正常

## 风险
- 无
