# 交付说明 — msg-2026-04-04T14-25-04-749Z

## 源任务
`msg-2026-04-04T14-25-04-749Z.md` — 控件检查器选中 TileView 后看不到任何属性。

## 目标
修复检查器无法正确关联和显示 TileView 控件属性的 bug。

## 根因分析
1. **标签不匹配**：树节点数据为 `"GridContainer (UTileView)"`，但 `TREE_LABEL_MAP` 中为 `'GridContainer (TileView)'`（缺 U 前缀），导致 `findDomByTreeLabel` 匹配失败，通过节点树点击 TileView 时返回 null，inspector 不渲染。
2. **遮挡层**：`inventoryPanelArt` 使用 `position: absolute; inset: 0` 覆盖整个 inventoryPanel 区域，可能拦截了本应落在 TileView 上的点击事件。

## 改动

### bag-preview-umg.html
- **修复 TREE_LABEL_MAP**：`gridContainer` 映射从 `'GridContainer (TileView)'` 改为 `'GridContainer (UTileView)'`，与树节点 label 一致。
- **修复 inventoryPanelArt CSS**：添加 `pointer-events: none`，确保点击穿透到下层 TileView。

## 验证
- HTTP 200，页面 48KB 正常加载。
- 节点树点击 "GridContainer (UTileView)" → 检查器显示完整属性面板（基本信息 + 布局 + 样式 + UMG 属性 + TileView 属性）。
- 画布直接点击物品格子区域 → 检查器正确选中 TileView 并显示属性。

## 风险
- 无。
