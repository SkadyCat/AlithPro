# 交付说明 — msg-2026-04-04T12-39-15-449Z

## 源任务
`msg-2026-04-04T12-39-15-449Z.md` — 节点树点击控件时支持反选。

## 目标
点击已高亮的节点树行时取消选中（反选），同时清除画布上的检查器高亮和属性面板；点击未选中节点则正常选中并联动画布。

## 改动

### application/elements/UTreeView.js
- 节点行点击逻辑增加 toggle：若已 `.highlighted` 则移除高亮并派发 `OnItemDeselected` 事件；否则高亮并派发 `OnItemClicked`。
- 每次点击先清除所有 `.highlighted`，确保单选。

### application/public/bag-preview-umg.html
- 新增 `REVERSE_TREE_MAP`：树节点 label → DOM 元素的反向映射。
- 新增 `findDomByTreeLabel(label)`：支持 ID 映射、Tab 映射、Item 槽位匹配。
- 监听 `OnItemClicked`：树节点点击时自动开启检查器、高亮对应 DOM 元素、渲染属性面板。
- 监听 `OnItemDeselected`：反选时清除画布高亮和属性面板。

## 验证
- UTreeView.js node --check 通过。
- HTTP 200，页面 32KB 正常加载。
- 树节点单击选中→再次单击反选，双向联动正常。

## 风险
- 无。
