源任务文件：`msg-2026-04-03T13-48-23-621Z.md`

任务目标：
- 将 `bag.bp` 生成对应的 Godot UI。

所做改动：
- 新增 `application\godot_inventory\tools\generate_bag_ui.gd`：
  - 读取 `bag.bp` 的 `[Tree]` 节点结构；
  - 在 Godot headless 环境中动态创建控件树；
  - 应用背包 UI 所需的布局、初始文本、贴图与按钮配置；
  - 保存为 `application\godot_inventory\scenes\bag_ui.tscn`；
  - 保存后重新加载场景并校验蓝图节点是否完整存在。
- 新增 `application\godot_inventory\tools\run_generate_bag_ui.js`，负责定位本机 Godot Console 并调用生成脚本。
- 更新 `application\package.json`，新增 `npm run generate:bag-godot-ui`。
- 更新 `application\godot_inventory\tools\build_bag_ui.js`，让 `npm run build:bag-ui` 在生成资源后也同步生成 `bag_ui.tscn`。
- 更新 `application\godot_inventory\project.godot`，将默认启动场景切换为 `res://scenes/bag_ui.tscn`。
- 更新 `application\godot_inventory\aigc\README.md`，补充 Godot 场景生成入口与产物说明。

落地结果：
- `bag.bp` 现在不仅能驱动资源清单和任务包，还能直接生成可打开的 Godot 场景：
  - 场景文件：`application\godot_inventory\scenes\bag_ui.tscn`
  - 命令入口：`npm run generate:bag-godot-ui`
  - 完整构建：`npm run build:bag-ui`
- `generated-assets.json` 中也已补充 `generatedScene: res://scenes/bag_ui.tscn`，方便追踪本轮 UI 落地产物。

验证结果：
- `npm run generate:bag-godot-ui` 通过，Godot Console 成功保存 `res://scenes/bag_ui.tscn`。
- `npm run build:bag-ui` 通过，资源与场景可一并生成。
- `npm run check` 通过。

剩余风险或阻塞项：
- 当前 `bag_ui.tscn` 的布局与控件属性来自生成脚本中的布局映射，不是一个完全通用的蓝图编译器；如果 `bag.bp` 未来扩展出新的控件类型或布局规则，需要继续扩展 `generate_bag_ui.gd`。
