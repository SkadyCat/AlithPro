源任务文件：`msg-2026-04-03T12-25-15-760Z.md`

任务目标：
- 根据 `application\godot_inventory\bag.bp` 生产其中声明的背包控件资源。

所做改动：
- 更新 `application\godot_inventory\bag.bp`，补入 `topbar_crest.png`，让资源清单与现有背包场景实际使用的贴图保持一致。
- 新增 `application\godot_inventory\tools\generate_bag_assets.js`：
  - 读取 `bag.bp` 中的 `res://assets/...` 资源声明；
  - 调用现有 `generate_oriental_inventory_assets.js` 与 `generate_oriental_chrome_assets.js`；
  - 在生成后校验 `bag.bp` 中列出的资源是否全部落盘。
- 更新 `application\package.json`，新增 `npm run generate:bag-assets`，把背包资源生成链封装成固定入口。

验证结果：
- `node --check godot_inventory\tools\generate_bag_assets.js` 通过。
- `npm run generate:bag-assets` 通过，脚本确认 `bag.bp` 中声明的 34 个资源全部存在。
- `npm run check` 通过。

剩余风险或阻塞项：
- 当前 `generate_bag_assets.js` 仍然复用两套既有东方风格资源生成脚本；如果后续 `bag.bp` 扩展到其他主题或新增资源类型，需要继续补充资源生成器与映射逻辑。
