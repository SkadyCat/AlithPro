源任务文件：`msg-2026-04-03T15-28-37-111Z.md`

任务目标：
- 基于当前项目里现有的资源，重新生成 Godot UI。

所做改动：
- 使用现有命令 `npm run generate:bag-godot-ui` 重新执行 `application\godot_inventory\tools\run_generate_bag_ui.js`。
- 由 `application\godot_inventory\tools\generate_bag_ui.gd` 重新生成 `application\godot_inventory\scenes\bag_ui.tscn`。
- 未修改蓝图结构和生成器逻辑，本次仅按当前已存在资源重生成场景。

验证结果：
- `npm run generate:bag-godot-ui` 通过，Godot 输出 `Generated Godot UI scene: res://scenes/bag_ui.tscn`。
- 使用 headless Godot 重新加载 `application\godot_inventory` 项目通过。
- 已确认 `application\godot_inventory\scenes\bag_ui.tscn` 存在。

剩余风险或阻塞项：
- 当前重生成基于项目现有 `assets\ui` 与 `assets\icons`，尚未切换到独立导出的 `exports\bag_undead_pack` 资源目录；如果后续要让场景正式使用那套导出包，还需要额外做资源替换或路径调整。
