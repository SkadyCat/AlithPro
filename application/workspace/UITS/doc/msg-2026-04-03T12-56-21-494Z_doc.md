源任务文件：`msg-2026-04-03T12-56-21-494Z.md`

任务目标：
- 把 “AIGC 生成 UI 的方案” 落地到当前仓库，形成可执行的本地**准备**流水线。

所做改动：
- 更新 `application\package.json`，新增 `npm run prepare:bag-aigc` 入口。
- 新增 `application\godot_inventory\tools\prepare_bag_aigc.js`：
  - 读取 `application\godot_inventory\bag.bp` 的资源声明；
  - 为每个资源生成标准化 AIGC 任务定义；
  - 输出统一的负面提示词、参考资源、后处理要求与批量生成建议；
  - 自动产出 `application\godot_inventory\aigc\bag-task-pack.json` 与 `application\godot_inventory\aigc\prompts\*.txt`。
- 新增 `application\godot_inventory\aigc\README.md`，说明这套 AIGC 准备层如何与现有 `generate:bag-assets` 资源脚本并行协作。

落地结果：
- 当前仓库已经具备一个平台无关的 AIGC 准备层：
  - `bag.bp` 继续作为背包 UI 资源需求源；
  - `prepare:bag-aigc` 负责把资源需求转成可投喂 ComfyUI / Scenario / 其他平台的任务包；
  - `generate:bag-assets` 继续作为确定性兜底与结构回归链。
- 实际生成产物包含：
  - 34 个资源任务；
  - 高/中优先级划分；
  - 每个资源的尺寸、策略、prompt、negative prompt、references、postprocess；
  - 可直接复制使用的逐资源 prompt 文本。

验证结果：
- `node --check godot_inventory\tools\prepare_bag_aigc.js` 通过。
- `npm run prepare:bag-aigc` 通过，成功生成 34 个资源任务包。
- `npm run check` 通过。

剩余风险或阻塞项：
- 当前落地的是 **AIGC 准备层与任务标准化**，还没有直接接入具体第三方平台 API，也**没有**实际调用任何模型生成 PNG。
- 如果下一步要做到“一键出图”，需要再选定具体执行端（如本地 ComfyUI、Scenario API 或其他模型服务），并补对应的调用适配器。
