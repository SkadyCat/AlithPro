源任务文件：`msg-2026-04-02T16-51-42-786Z.md`

任务目标：
- 做几个不同风格的背包界面，并能在当前工作台中切换预览。

所做改动：
- 更新 `application\public\app.js`：
  - 在 `screens` 中新增 3 套不同风格的背包界面：
    - `国风背包`
    - `科幻背包`
    - `轻日式旅包`
  - 为每套背包增加独立的层级说明，右侧 `right_info` 会随切换同步变化。
  - 新增 3 个独立的 canvas 绘制函数：
    - `drawGuofengInventory()`
    - `drawSciFiInventory()`
    - `drawLightInventory()`
  - `left_menu` 现在可以直接切换这些背包变体，并在 `center_area` 中看到对应风格的真实预览。

效果结论：
- 现在不再只有一套背包页，而是能直接横向比较不同题材下的背包界面：
  - `国风背包`：器物感、暖色、卷轴分类
  - `科幻背包`：模块仓、扫描感、冷色系统面板
  - `轻日式旅包`：更明快、轻松、旅途道具导向

验证结果：
- `npm run check` 通过。
- 页面结构保持 `left_menu + center_area + right_info`，并继续通过 `http://127.0.0.1:3200/` 访问。

剩余风险或阻塞项：
- 当前这些背包变体已经能用于比较方向，但仍是同一套 canvas 技术框架下的静态预览；若要继续深入，需要为每种风格分别补充图标系统、材质语言和交互动效。
