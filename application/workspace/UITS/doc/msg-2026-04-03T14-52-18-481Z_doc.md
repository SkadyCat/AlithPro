源任务文件：`msg-2026-04-03T14-52-18-481Z.md`

任务目标：
- 按照当前这套“干净 + 轻度亡灵风”的视觉方向，重新生成 `bag.bp` 对应的素材。
- 把生成结果放进一个独立文件夹，避免覆盖现有正式资源。

所做改动：
- 新增 `application\godot_inventory\tools\generate_bag_undead_assets.js`：
  - 读取 `application\godot_inventory\bag.bp` 的资源声明；
  - 为 `bag.bp` 中声明的 34 个资源建立尺寸与种类映射；
  - 以冷灰绿、骨白描边、幽绿光感的“亡灵风”重新绘制整套 UI 与图标资源；
  - 输出到独立目录 `application\godot_inventory\exports\bag_undead_pack\assets\ui` 与 `assets\icons`；
  - 同步写出 `manifest.json` 和一份 `bag.bp` 副本，方便整包查看和后续接入。
- 更新 `application\package.json`：
  - 新增命令 `npm run generate:bag-undead-assets`。

导出目录：
- `application\godot_inventory\exports\bag_undead_pack\`

验证结果：
- `node --check godot_inventory\tools\generate_bag_undead_assets.js` 通过。
- `npm run generate:bag-undead-assets` 通过。
- `npm run check` 通过。
- 导出结果已确认：
  - `manifest.json` 中 `assetCount=34`
  - `assets` 清单条目数为 34
  - `assets\ui` 下 22 个文件
  - `assets\icons` 下 12 个文件
  - 导出目录内包含 `bag.bp` 副本

剩余风险或阻塞项：
- 当前导出的是一套独立对比包，尚未切换 Godot 场景默认引用路径；如果后续要正式启用这套亡灵风素材，还需要再做资源替换或场景指向调整。
